# 92I-1723102 表面积比对报告的反向复核

## 1. 复核结论

本次复核对象：

- [dsh的表面积结果.md](./dsh的表面积结果.md)：估算 `280～310 cm²`，取中值约 `300 cm²`；
- [计算结果比对情况.md](./计算结果比对情况.md)：判定 `300 cm²` 比 `248.3 cm²` 更合理；
- [表面积计算.md](./表面积计算.md)：尺寸重建结果为 `248.308674 cm²`。

反向复核后的判定是：

> **仅依据当前 PDF 图纸，`248.3 cm²/件` 比 `300 cm²/件` 更有图纸依据，也更可复算。dsh 的比对报告提出“应进行质量闭合校验”是合理的，但据 `171.05g` 反推出 `Ø50` 级贯通内腔的关键推理不成立，因此“300 cm² 更准确”的最终结论不合理。**

`248.3 cm²` 仍是二维图纸下的工程模型值，不应把小数点后六位理解为实物测量精度。若取得 STEP/IGES 或真实称重数据，仍应重新校核。

## 2. dsh 报告中合理的部分

以下意见应当保留：

1. 对表面积计算做体积、质量的交叉检查，是发现漏孔、漏腔或截面误读的有效方法；
2. 化学镀面积是否包含孔、盲孔、螺纹和可能的内腔，必须与实际屏蔽及工艺要求一致；
3. `248.3 cm²` 对外槽、异形孔及倒角的重建比 dsh 的圆柱近似细，但仍不是原生三维 CAD 的面积属性；
4. 最终精确值应优先由三维模型直接给出，并用三维体积和正确材料密度复核。

dsh 的问题不在于提出质量校验，而在于把标题栏质量视为绝对正确，同时忽略了它与剖视图及 CAD 几何体积之间更直接的数值关系。

## 3. 决定性问题一：`171.05g` 的解释反了

### 3.1 用 `248.3 cm²` 所采用的截面反算几何体积

使用同一套图纸截面，并把后段两槽及 `1×30°` 外倒角计入，体积分项如下：

| 体积分项 | 计算值（mm³） |
|---|---:|
| 后段开槽截面 `1,028.461879×6.2` | 6,376.463648 |
| 两槽大径截面 `2,272.515213×58.5` | 132,942.139948 |
| `Ø39.975` 前段圆柱及 `1×30°` 圆台 | 33,599.884811 |
| 右端外轴 `353.806678×25` | 8,845.166952 |
| 扣除异形盲孔 `384.764250×26` | -10,003.870500 |
| 扣除 `Ø28×90°` 入口增加的空腔 | -408.431153 |
| 扣除 `Ø4.13` 底孔、`Ø7×90°` 及钻尖 | -249.733907 |
| **几何体积合计** | **171,101.619800** |

即：

```text
V几何 = 171,101.619800 mm³
      = 171.101620 cm³
```

将标题栏的数值除以该几何体积：

```text
ρ标题栏隐含 = 171.05 g / 171.101620 cm³
             = 0.99970 g/cm³
```

`171.101620 cm³` 与标题栏数字 `171.05` 的差仅约 `0.03%`。这不是普通的偶然接近，它强烈表明标题栏质量极可能由 CAD 在约 `1.0 g/cm³` 的默认或错误材料密度下自动生成，或者该栏未在材料改为 45# 后刷新。

若强行采用 45# 钢密度 `7.85 g/cm³`，同一图纸几何对应：

```text
m = 171.101620×7.85
  = 1,343.15 g
```

因此，PDF 内部实际存在一组冲突信息：技术要求写 45#，但标题栏 `171.05g` 与图示几何更像按约 `1 g/cm³` 生成。出现这种冲突时，不能绕过剖视图，凭一个可能失效的质量字段创造图纸没有标注的 `Ø50` 内腔。

### 3.2 dsh 实际上也算出了同一个提示，却作了相反解释

《计算结果比对情况.md》给出的原模型体积约为 `171.80 cm³`。该数字与标题栏 `171.05` 同样近似一一对应。报告随后直接乘以钢密度得到约 `1,348.6 g`，并据此认定模型错误；但它没有先检查为什么“体积的 cm³ 数值”会几乎等于“质量栏的 g 数值”。

更合理的处理顺序应当是：

1. 先检查剖视图是否确实画出并标注了大内腔；
2. 再检查 CAD 材料属性、标题栏质量是否更新；
3. 只有质量字段经过实物称重或正确材料属性确认后，才可用于反推缺失结构。

当前 PDF 不满足第 1、2 项，所以 `171.05g` 不能作为“存在 Ø50 内腔”的决定性证据。

## 4. 决定性问题二：`Ø50.39` 反推没有完成体积闭合

dsh 以 45# 钢密度计算：

```text
V目标 = 171.05/7.85
      = 21.79 cm³
```

随后用 `Ø54.9×58.4` 外圆段反推 `Ø50.39` 内孔。该薄壁主段本身的材料体积为：

```text
V主段薄壁 = π/4×(54.9²-50.39²)×58.4
           = 21,780.43 mm³
           = 21.78043 cm³
```

也就是说，仅这个假设的薄壁主段就已经耗尽几乎全部 `21.79 cm³` 目标体积，尚未加入后段、`Ø39.975` 前段和右端外轴。

按同一图纸模型，把除主段以外的已画明结构以及原有盲孔一起计入，其净体积约为：

```text
V其余净值 = 38,159.48 mm³

V假设总值 = 21,780.43+38,159.48
           = 59,939.91 mm³
           = 59.93991 cm³

m假设总值 = 59.93991×7.85
           = 470.53 g
```

仍然是标题栏 `171.05g` 的约 `2.75` 倍。尤其是 `Ø39.975` 前段及其倒角自身几何体积约为 `33.60 cm³`，按 45# 钢已经约 `263.8 g`，还没有计入其他结构。

因此，dsh 所称“方向与质量证据一致”并不等于完成了质量闭合。`Ø50.39` 是把整件允许的材料体积错误地全部分配给一个轴段后得到的数值，不能用于证明 `300 cm²`。

## 5. 决定性问题三：剖视图不支持大贯通内腔

图纸 A-A 轴向剖视中：

- 除左端深 `26 mm` 的异形盲孔和其孔底 `M5/Ø4.13` 外，主体中心区域连续画有剖面线；
- 若存在 `Ø50` 级内腔，标准剖视中应出现大面积不剖区域、内壁轮廓线和孔径/深度标注，图中均没有；
- 假设孔径 `Ø50.39`，其半径为 `25.195 mm`，已经大于两端 `Ø36.4` 和 `Ø39.975` 外径的半径，故不可能作为一个从两端贯通且保持壁厚的孔；
- 若把它限制在 `Ø54.9` 主段内部，则会成为图中未画出、未标尺寸且难以与外界连通的封闭大腔。封闭腔既不能凭图纸制造，也不能直接作为化学镀可达表面计价；
- 等轴测图显示的是带两条纵槽的实体轮廓和左端盲孔，也没有显示一层约 `2.25 mm` 厚的薄壁筒。

所以，“标题质量小”只能说明标题质量、材料属性或图纸信息至少有一项存在问题，不能凌驾于明确的剖视表达之上。

## 6. 若干尺寸被误当成独立孔系

dsh 的计算把 `4×Ø5`、`2×Ø3.57`、`Ø24.41`、`2×16.5` 等列入“各钻/攻孔”。这与端视图的标注对象不符：

| 图纸标注 | 在轮廓中的作用 | 是否应另加为独立轴向孔 |
|---|---|---|
| `2×3.57` | 两条径向/纵向槽的槽宽 | 否，已进入开槽截面的面积和周长 |
| `2×16.5` | 两处槽根相对轴线的轮廓尺寸 | 否 |
| `4×R3.6` | 两条槽对应的四处轮廓圆角 | 否 |
| `4×Ø5` | 异形孔四个角部释放圆/圆弧特征 | 否，应并入同一个异形盲孔轮廓 |
| `2×8`、`2×16` | 角部特征的位置/间距尺寸 | 否 |
| `□18.97` 与 `Ø24.41` | 共同定义右端外轴复合截面 | 否，不是 `Ø24.41` 内孔 |
| `M5×0.8`、`Ø4.13`、`Ø7×90°` | 同一个攻丝底孔及入口倒角 | 是孔表面，但只能计一次 |

将轮廓尺寸再次当成若干钻孔，不仅没有图纸依据，还会与已计算的槽壁、异形孔壁或外轴表面重复。

## 7. dsh 比对表的分组口径不一致

dsh 的“外表面合计约 `20,228 mm²`”已经包含：

- 方舌侧壁；
- 台肩；
- 两端面。

但《计算结果比对情况.md》先把该总数与 `248.3` 模型中**不含右端外轴**的“主体外表面、台阶及端面 `19,847 mm²`”比较，下一行又单独比较右端外轴，形成了不一致的分组。

统一口径后的正确对比为：

| 分组 | dsh 估算（cm²） | `248.3` 模型（cm²） |
|---|---:|---:|
| 全部外表面，含右端外轴 | 约 202.27 | 219.927370 |
| 图示异形盲孔及 M5 底孔 | 未完整逐项计算 | 28.381304 |
| 假设的 `Ø50` 大内腔 | 约 91.7～92.5 | 0（图纸未显示） |
| 总计 | 取经验区间中值约 300 | 248.308674 |

因此，`248.3` 模型的外表面实际比 dsh 的外表面圆柱近似多约：

```text
219.927370-202.27 = 17.657370 cm²
```

主要因为开槽截面周长 `207.843 mm` 明显大于光滑 `Ø54.9` 圆周 `172.474 mm`，槽壁不能省略。dsh 报告中“外部约多计 8 cm²”等表述与其自身分项表不能闭合。

## 8. `300 cm²` 不是一个由图纸闭合得到的结果

dsh 的 `300 cm²` 是 `280～310 cm²` 区间的中值，其核心增加项为：

```text
π×约50×58.4 ≈ 9,170 mm²
```

该式只计算了一个假设圆柱内壁，尚未说明：

- 内腔从哪一端开口；
- 与 `Ø36.4`、`Ø39.975` 小外径段如何连接；
- 是否替代图示的深 `26 mm` 异形盲孔；
- 开口端面需要扣除多少面积；
- 是否为可进入镀液的开放表面；
- 为什么剖视图没有内壁及尺寸。

所以该 `91.7 cm²` 不能直接加到外表面上，`300 cm²` 也不是一个可从图纸逐项复算的确定值。

## 9. 对 `248.3 cm²` 结果的自我纠正与局限

`248.3 cm²` 更符合当前图纸，不表示原计算没有需要改进之处：

1. 原文直接写“报价采用 248.3 cm²/件”略显确定。更严谨的说法应是“当前二维图纸工程基准值为约 248.3 cm²/件”；
2. 原计算没有在交付文档中列出体积及标题栏隐含密度校验，导致 `171.05g` 的异常没有提前解释；这是应当补充的检查项；
3. `M5×0.8` 按 `Ø4.13` 底孔简化，未展开真实螺纹牙侧面积，可能略低估；
4. `R1.8`、`R2.5`、`R5` 及若干轴向交界采用工程等效，二维轮廓提取也不等于原生三维曲面；
5. 右端外轴的铣削收尾圆角、全部小过渡面及实际镀层可达性，仍应由三维模型确认；
6. `24,830.867371 mm²` 是计算模型的数值精度，合理对外表达仍是约 `248.3 cm²`，不是六位小数的实物精度。

这些已知简化通常影响局部圆角、螺纹和收尾曲面，不能在没有大内腔图纸证据的情况下解释 `51.7 cm²` 的差值。

## 10. 最终准确性判定

| 判定维度 | dsh：约 `300 cm²` | 尺寸重建：约 `248.3 cm²` |
|---|---|---|
| 是否直接来自已标注几何 | 否，主要差量来自假设 `Ø50` 内腔 | 是，按端视轮廓和轴向尺寸逐项重建 |
| 与 A-A 剖视图是否一致 | 不一致，剖视未显示大内腔 | 一致，主体按剖面线所示实体处理 |
| 内外表面是否可逐项闭合 | 否，`300` 为区间中值 | 是，三组相加闭合为 `248.308674` |
| 质量校验 | 使用了不可靠的标题质量，且 `Ø50.39` 模型仍未闭合 | 几何体积约 `171.102 cm³`，揭示标题栏隐含密度约为 `1.0 g/cm³` |
| 未知条件 | 内腔直径、开口、深度、连接及可镀性全部未知 | 小圆角、真实螺纹和三维收尾仍需确认 |

综合判定：

```text
当前 PDF 证据下的优先结果：约 248.3 cm²/件
dsh 的 300 cm²：不应作为确定报价面积
```

只有在后续 STEP/IGES、补充剖视图或实物检测明确证明存在开放且需要施镀的大内腔时，才应推翻 `248.3 cm²` 并重新计算；即使实物确重 `171.05g`，也只能证明当前二维模型或图纸信息不完整，不能自动证明 dsh 假设的 `Ø50.39×58.4` 内腔正确。

## 11. 建议的最终验证

按优先级执行：

1. 从原始 CAD 读取 `Volume`、`Surface Area`、材料名称和密度，确认标题栏质量是否随材料属性联动；
2. 获取 STEP/IGES，分别输出“全部几何表面”和“实际需镀表面”；
3. 实物称重：若约 `1.34 kg`，基本确认标题栏密度/质量字段错误；若确为约 `171 g`，则必须补充当前 PDF 未表达的减重结构后再算，两个现有结果都不能直接定案；
4. 明确盲孔、M5 螺纹及槽底是否施镀或屏蔽；
5. 工艺损耗、挂具及返工余量在几何面积之外单列。
