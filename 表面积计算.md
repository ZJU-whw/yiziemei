# 92I-1723102（92I KT20 SR 弹簧轴）表面积计算

## 1. 结论

按图纸公称尺寸和下述工程简化口径，单件理论可镀表面积为：

```text
S = 24,830.867371 mm²
  =    248.308674 cm²
  =      0.024830867 m²
```

因此，化学镀报价采用 **248.3 cm²/件**（即 **2.483 dm²/件**）。若报价制度要求取整，可另按制度取值；挂具带出、返工和工艺损耗系数应在该几何面积之外另加，不能混入零件理论面积。

## 2. 图纸与核算口径

- 图号：`92I-1723102`。
- 名称：`92I KT20 SR 弹簧轴`。
- 复核图纸标题：`92I-1723102  92I KT20 SR弹簧轴 - 图.pdf`。仓库历史中的文件名为 `92I-1723102  92I KT20 SR.pdf`，来源提交为 `d2f4dc28681ada38e47d45bfa8c53c54a07529fa`，PDF Blob 为 `49b983d2a420a2f94c54639f3296235c5a1aba62`。
- 采用镀后公称尺寸计算，不把尺寸公差、粗糙度和 `0.025～0.035 mm` 镀层厚度再次叠加到几何尺寸中。
- 计入开槽外轮廓、槽壁、圆柱面、台阶端面、异形盲孔侧壁及孔底、`Ø28×90°` 和 `Ø7×90°` 入口锥面、右端外轴侧面及端面。
- `M5×0.8` 不展开真实螺旋牙型，按图样简化为 `Ø4.13` 攻丝底孔；钻尖按常用 `118°` 钻尖，即半角 `59°` 估算。
- 未注小圆角及轴向复合交界处按相邻公称截面作工程等效。故本结果适合询价、报价和槽液负荷估算，不替代三维 CAD 的高精度 Surface Area 报告。
- 异形轮廓均先按图纸标注尺寸重建，再求面积和周长；不直接用 PDF 页面像素比例计算。

## 3. 基本方法

等截面沿轴向拉伸长度 `L` 时，侧面积为：

```text
S侧 = P截面 × L
```

台阶端面按相邻两个截面的外露区域计算。若两个截面不互为包含关系，则：

```text
S台阶 = A1 + A2 - 2 × A交集
```

由直线和圆弧组成的异形截面，周长为各线段、圆弧长度之和，面积按轮廓积分求得：

```text
A = 1/2 × |∮(x dy - y dx)|
```

所有分项先保留未取整数值，最后统一求和并换算单位。

## 4. 异形截面中间量

### 4.1 两槽大径截面

取图示参数：

```text
外圆半径 R  = 54.9 / 2 = 27.45 mm
槽根半径 r0 = 16.5 mm
槽宽 b       = 3.57 mm
外缘圆角 rf  = 3.6 mm
槽数 n       = 2
h             = b / 2 = 1.785 mm
xf            = h + rf = 5.385 mm
yf            = √[(R-rf)²-xf²] = 23.234119 mm
δ             = atan(xf/yf) = 13.049112°
ε             = asin(h/r0) = 6.210516°
y0            = √(r0²-h²) = 16.403164 mm
```

一个开槽缺口由外圆弧、两个 `R3.6` 圆角、两条直边及槽根圆弧围成。按上述轮廓积分：

```text
A单槽 = 47.341353 mm²

A大径 = πR² - 2A单槽
      = π×27.45² - 2×47.341353
      = 2,272.515213 mm²
```

周长中扣除两个槽口所替代的外圆弧，再加上各槽的圆角、直边及槽根圆弧：

```text
P大径 = R(2π-4δ)
      + 2[2rf(π/2+δ) + 2(yf-y0) + 2r0ε]
      = 207.843379 mm
```

### 4.2 后段 `Ø36.4` 开槽截面

后段半径 `Rb=36.4/2=18.2 mm`，两个径向槽仍取 `b=3.57 mm`、槽根半径 `r0=16.5 mm`。将同一槽轮廓裁剪到 `Rb`，原计算离散精度下每槽面积为 `6.079636 mm²`：

```text
A后 = π×18.2² - 2×6.079636
    = 1,028.461879 mm²
```

令：

```text
α = asin(h/Rb)
s = √(Rb²-h²) - √(r0²-h²)
```

则：

```text
P后 = 2πRb + 2[-2Rbα + 2s + 2r0ε]
    = 121.192842 mm
```

### 4.3 四圆角异形盲孔

依据端视图的直线和圆弧轮廓，按标注尺寸重建并积分，得到：

```text
A孔 = 384.764250 mm²
P孔 = 82.114000 mm
```

### 4.4 右端外轴复合截面

外轴截面为 `□18.97` 与 `Ø24.41` 的相交轮廓。令正方形半边长 `a=18.97/2=9.485 mm`，圆半径 `Rz=24.41/2=12.205 mm`，圆与正方形边的交点半坐标为：

```text
t = √(Rz²-a²) = 7.680937 mm
```

定义圆面积积分原函数：

```text
F(x) = 1/2[x√(Rz²-x²) + Rz² asin(x/Rz)]
```

则复合截面的面积和周长为：

```text
A轴 = 4[a×t + F(a)-F(t)]
    = 353.806678 mm²

P轴 = 4×(2t) + 4Rz[π/2-2atan(t/a)]
    = 71.671492 mm
```

### 4.5 其余计算中间量

| 中间量 | 算式或含义 | 数值 |
|---|---|---:|
| 前段圆柱截面积 `A前` | `π×(39.975/2)²` | `1,255.066756 mm²` |
| 30° 倒角末端半径 `re` | `39.975/2-tan30°×1` | `19.410150 mm` |
| 倒角末端截面积 `Ae` | `π×re²` | `1,183.607324 mm²` |
| 大径轮廓与前段圆的交集 `Ai` | 两轮廓求交后积分 | `1,230.126055 mm²` |
| `Ø28` 入口投影面积 | `π×14²` | `615.752160 mm²` |

## 5. 主体外表面、台阶和端面

轴向长度由图纸尺寸链得到：

```text
后段长度   L后   = 91.5-85.3 = 6.2 mm
大径段长度 L大径 = 85.3-26.8 = 58.5 mm
前段长度   L前   = 26.8 mm
外轴长度   L轴   = 25 mm
```

### 5.1 后段开槽柱面

```text
S1 = P后×L后
   = 121.192842×6.2
   = 751.395620 mm²
```

### 5.2 开槽大径段侧面

```text
S2 = P大径×L大径
   = 207.843379×58.5
   = 12,158.837663 mm²
```

### 5.3 前段圆柱面和 `1×30°` 倒角面

倒角轴向长度为 `1 mm`，径向变化和斜长为：

```text
Δr = tan30°×1 = 0.577350 mm
l  = √(1²+Δr²) = 1.154701 mm
```

因此：

```text
S3 = 2π×19.9875×(26.8-1)
   + π×(19.9875+19.410150)×1.154701
   = 3,383.016155 mm²
```

### 5.4 两处截面过渡台阶

后段开槽截面完全包含在大径开槽截面内：

```text
S4a = A大径-A后
    = 2,272.515213-1,028.461879
    = 1,244.053334 mm²
```

大径开槽截面与前段圆截面不互为包含关系，取两轮廓的对称差：

```text
S4b = A前+A大径-2Ai
    = 1,255.066756+2,272.515213-2×1,230.126055
    = 1,067.329858 mm²

S4 = S4a+S4b
   = 2,311.383192 mm²
```

### 5.5 左端面（扣除 `Ø28` 入口）

```text
S5 = A后-π×14²
   = 1,028.461879-615.752160
   = 412.709719 mm²
```

### 5.6 右侧外轴轴肩端面

```text
S6 = Ae-A轴
   = 1,183.607324-353.806678
   = 829.800646 mm²
```

主体外表面、台阶及端面小计：

```text
S主体 = S1+S2+S3+S4+S5+S6
      = 751.395620
      + 12,158.837663
      + 3,383.016155
      + 2,311.383192
      + 412.709719
      + 829.800646
      = 19,847.142995 mm²
```

## 6. 左端异形盲孔及 `M5` 底孔

### 6.1 `Ø28×90°` 入口锥面

由于锥面接异形孔，不能用普通圆台公式直接代替。设异形孔轮廓在方向 `θ` 上的极径为 `ρ(θ)`，`Ø28` 入口半径 `Rm=14 mm`。对重建轮廓积分得到：

```text
入口投影面积     A入口 = πRm² = 615.752160 mm²
90°锥面面积      S28   = (√2/2)∫[Rm²-ρ(θ)²]dθ
                       = 326.666039 mm²
锥面占用的孔壁面积 S扣 = ∮[Rm-ρ]ds
                       = 222.840404 mm²
```

### 6.2 `Ø7×90°`、`Ø4.13` 底孔及钻尖

```text
底孔半径 r = 4.13/2 = 2.065 mm
入口半径 R = 7/2 = 3.5 mm
90°入口轴向深度 h7 = R-r = 1.435 mm
90°入口斜长 l7 = √2×h7 = 2.029396 mm
钻尖高度 ht = r/tan59° = 1.240777 mm
```

各表面为：

```text
S7锥 = π(R+r)l7
     = 35.479863 mm²

S底孔柱面 = π×4.13×(17-h7)
           = 201.952414 mm²

S钻尖 = πr√(r²+ht²)
      = 15.628755 mm²

SM5简化 = S7锥+S底孔柱面+S钻尖
         = 253.061033 mm²
```

### 6.3 盲孔小计

异形孔深度为 `26 mm`。其侧壁先按 `P孔×26` 计算，再扣除被 `Ø28×90°` 锥面占用的孔壁；孔底面积须扣除中央 `Ø7` 入口：

```text
S盲孔 = (P孔×26-S扣) + S28
      + (A孔-π×3.5²) + SM5简化

      = (82.114×26-222.840404) + 326.666039
      + (384.764250-38.484510) + 253.061033

      = 2,838.130408 mm²
```

## 7. 右端外轴

```text
S外轴侧面 = P轴×25
          = 71.671492×25
          = 1,791.787290 mm²

S外轴端面 = A轴
          = 353.806678 mm²

S外轴 = 1,791.787290+353.806678
      = 2,145.593968 mm²
```

## 8. 汇总及单位换算

| 分组 | 表面积（mm²） | 表面积（cm²） |
|---|---:|---:|
| 主体外表面、台阶及端面 | 19,847.142995 | 198.471430 |
| 左端异形盲孔及 `M5` 底孔 | 2,838.130408 | 28.381304 |
| 右端外轴 | 2,145.593968 | 21.455940 |
| **合计** | **24,830.867371** | **248.308674** |

```text
S总 = 19,847.142995+2,838.130408+2,145.593968
    = 24,830.867371 mm²

1 cm² = 100 mm²
S总 = 24,830.867371 / 100
    = 248.308674 cm²
    ≈ 248.3 cm²/件

1 m² = 1,000,000 mm²
S总 = 0.024830867 m²/件
```

## 9. 报价使用提示

- 单件几何面积采用 `248.3 cm²`；批量 `N` 件时，理论总面积为 `0.024830867×N m²`。
- 若盲孔、底孔或螺纹要求封堵、不镀，应按实际不镀范围从相应分项中扣除，不能继续直接使用本结果。
- 若需核算实际药液消耗，应另外考虑挂具面积、带出、返工率和企业工艺余量。
- 若需把真实 `M5×0.8` 牙侧面积、全部过渡圆角和镀层厚度变化计入，应以原始三维模型重新提取 Surface Area。
