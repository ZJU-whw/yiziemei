# M_TSGZ_TJBB 出口退税分类情况统计态势感知——系统功能分析整理

> 文档编号：`FUN-M_TSGZ_TJBB`
> 当前版本：V0.1（静态资料还原稿）
> 整理日期：2026-09-03
> 代码与资料基线：`main@9648d427f49f4aa0d6faef8ec78f3959cb5dc3ee`
> 状态：**静态分析基线；运行口径、数据权限及缺失服务实现仍需业务、开发和数据平台主管确认**

---

## 功能简述

1. **[事实]** `M_TSGZ_TJBB` 是“态势感知 → 出口退税分类情况统计态势感知”路径下的有效叶子菜单，前端通过 `component=-1` 打开外部地址 `/jdgl/dashboard/tjbb`。需求资料说明：出口退税月报表的态势感知。 主要包括商品分布情况、海关分布情况、监管方式分布情况、贸易国分布情况，具体数据来源于当月省局月报表。

---

## 1. 文档与功能身份

| 项目 | 内容 | 证据状态 |
|---|---|---|
| 功能编号 | `FUN-M_TSGZ_TJBB` | 本文建立 |
| 菜单代码 | `M_TSGZ_TJBB` | [事实] |
| 功能名称 | 出口退税分类情况统计态势感知 | [事实] |
| 菜单路径 | `M_ROOT → M_TSGZ → M_TSGZ_TJBB` | [事实]，菜单结构表 |
| 页面入口 | 前端通过 `component=-1` 打开外部地址 `/jdgl/dashboard/tjbb` | [事实]，`app.js` |
| 功能类型 | 外部可视化页面 | [事实]，页面源码与需求资料 |
| 需求来源/年份 | 《便捷退税税务端功能清单》未单列该叶节点的提出方和年份 | [事实/待确认] |
| 原关系表服务标识 | `jdgl/dashboard/tjbb/` | [事实]，功能—数据库关系表 |
| 授权角色 | 角色矩阵未标记专属角色 | [事实]，菜单—角色关系表 |
| 菜单状态 | `ISVALID=1`、`CHILDFLAG=0` | [事实]，有效且无下级菜单 |
| 名称一致性 | 菜单表为“出口退税分类情况统计态势感知”，前端注册标题为“态势感知出口退税分类情况统计” | [风险]，需确认统一展示名称 |

---

## 2. 业务规则清单

| 规则编号 | 规则 | 来源 | 改造要求/待确认 |
|---|---|---|---|
| `RULE-TSGZ_TJBB-001` | 授权菜单包含 `M_TSGZ_TJBB` 时才应显示并允许打开该功能 | 菜单表、`app.js` | 服务端接口必须独立鉴权，不能只依赖前端隐藏 |
| `RULE-TSGZ_TJBB-002` | 出口退税月报表的态势感知。 主要包括商品分布情况、海关分布情况、监管方式分布情况、贸易国分布情况，具体数据来源于当月省局月报表。 | 功能清单 | 作为业务验收口径；歧义项由业务部门确认 |

---

## 3. 数据结构、血缘与一致性

### 3.1 核心实体

| 实体/对象 | Schema | 用途 | 操作 | 依据 |
|---|---|---|---|---|
| 未明确 | 未明确 | 从备案单证数字化管理态势感知“出口退（免）税情况可以跳转过来 | 待确认 | 功能—数据库关系表未形成可验证对象映射 |

### 3.2 核心表结构特征

- **[待确认]** 现有关系资料没有给出可验证的表级数据映射，需从生产接口 SQL、Mapper 或数据库访问审计补齐。

### 3.3 数据血缘

```mermaid
flowchart LR
    A[菜单 M_TSGZ_TJBB] --> B[外部页面<br/>/jdgl/dashboard/tjbb]
    B --> E[出口退税分类情况统计态势感知展示]
```

**[事实]** 功能—数据库关系表登记的基础对象为 `未登记`，业务对象为 `从备案单证数字化管理态势感知“出口退（免）税情况可以跳转过来`。
**[事实]** 页面及直接关联组件共识别 2 个接口/外部入口，其中 0 个可与当前提交的 `tl-bjts-sw` Controller 路由静态匹配；其余实现可能位于未提交服务、网关或外部系统。
**[待确认]** 静态匹配不等同于生产调用闭环，仍需核对网关前缀、实际 SQL、数据权限、刷新作业、异常补偿和对账机制。

---

## 4. 接口、文件与消息

### 4.1 页面直接依赖的接口

| 接口 | 方法 | 用途 | 服务端证据 | 改造关注点 |
|---|---|---|---|---|
| `/jdgl/dashboard/tjbb` | OPEN | 打开外部功能页面 | 外部页面；本仓库未包含其实现 | 确认单点登录、跨域、目标系统权限、超时与可用性监控 |
| `/auth/getmenu` | POST | 登录后取得授权菜单 | **当前服务工程未检出匹配路由** | 校验可见范围、字典有效性、缓存刷新和参数白名单 |

### 4.2 返回码与异常

当前主页面源码未形成可统一证明的返回码处理模式；公共 API 包装器采用业务码 `"0"` 判定成功。[事实/待确认]

以下接口契约仍需统一确认：

- HTTP 状态码与业务码的对应关系，以及未登录、无菜单权限、无数据权限的标准返回；
- 参数非法、页大小超限、排序字段非法、重复提交和并发修改的错误码；
- 正常空结果与上游数据未就绪、部分数据失败之间的区分；
- 请求关联号、用户提示、运维日志及敏感信息过滤规则。

### 4.3 文件、消息与外部接口

- 主页面及直接关联组件未检出明确的文件导入、导出或下载端点。[静态检索结论]
- 未发现该页面直接发送或消费 MQ 消息；若服务端通过异步作业生成数据或文件，需补充调度与消息链路证据。[静态检索结论/待确认]
- 菜单直接打开外部地址 `/jdgl/dashboard/tjbb`，需确认单点登录、跨域、目标系统权限、可用性监控及数据责任边界。[事实/待确认]

---

## 5. 实现资产清单与可追溯关系

### 5.1 前端资产

| 资产 | 关键位置/用途 |
|---|---|
| [`app.js`](../src/client/src/app.js) | 33 行菜单注册、页面组件/外部地址及授权过滤 |
| 外部地址 `/jdgl/dashboard/tjbb` | 当前仓库仅保存跳转配置，不含目标页面源码 |

### 5.2 服务端及数据库资产

| 资产 | 关键位置/用途 |
|---|---|
| 服务标识 `jdgl/dashboard/tjbb/` | 功能—数据库关系表登记的原部署/服务边界；与当前提交工程是否一致需确认 |
| 数据库实现资产 | 当前资料未形成表级映射，需补充 Mapper/SQL、生产对象及作业定义 |

### 5.3 需求—实现—数据—测试追踪

| 需求编号 | 需求 | 页面/接口 | 数据对象 | 验收用例 |
|---|---|---|---|---|
| `REQ-TSGZ_TJBB-001` | 授权用户可从菜单路径打开功能 | `M_TSGZ_TJBB`、`-1` | 菜单/角色配置 | `TC-01` 菜单可见性与接口越权校验 |
| `REQ-TSGZ_TJBB-002` | 外部可视化页面 | `/jdgl/dashboard/tjbb` | `待补证数据源` | `TC-02` 正常、空结果及边界条件 |
| `REQ-TSGZ_TJBB-003` | 查询条件、列表字段和业务口径一致 | 主组件及直接接口 | `待补证数据源` | `TC-03` 字段映射、分页排序及数据范围 |
| `REQ-TSGZ_TJBB-004` | 角色与税务机关数据范围受控 | `/auth/getmenu`、全部业务接口 | 权限对象待补证 | `TC-04` 横向/纵向越权与敏感字段校验 |
