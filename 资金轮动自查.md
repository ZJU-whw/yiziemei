# 资金轮动自查

> 自查对象：《A股板块潜力与龙头图形股研究报告》资金轮动频道
> 重点样本：同花顺行业“半导体（881121）”
> 对比日期：2026-08-31、2026-09-01
> 自查时间：2026-09-02（Asia/Shanghai）
> 结论等级：**阻断级数据质量异常（BLOCKING_DATA_QUALITY）**

## 一、结论先行

用户的质疑成立。

“指数下跌但资金净流入”在统计上并非绝对不可能：价格由边际成交决定，资金流向又是供应商按成交方向和订单特征估算出的代理指标；三日累计值也可能被前两个交易日主导。因此，不能仅凭 9 月 1 日指数下跌就断言三日资金净额一定为负。

但是，本次问题并不止于“价格与资金背离”。对已发布报告进行跨日、跨窗口和全行业复核后，可以确认：

1. 报告中的 `3日/5日/10日/20日净额` 不能通过现有证据证明是截止报告日的最近 N 个交易日累计值。
2. 多个日期、窗口之间出现整组数据的精确复制或错位，涉及全部 90 个行业，不是半导体单点异常。
3. 半导体 8 月 31 日至 9 月 1 日的三日净额变化，与滚动窗口恒等式及公开盘面方向均明显冲突。
4. 当前发布审计虽然显示 `PASS`，但只覆盖结构、语义字段完整性和离线渲染，没有检查供应商数据日期、滚动窗口恒等式、跨窗口累计关系或异常重复。
5. 在补齐逐日原始数据并重新计算之前，报告中的资金净额、资金阶段、价资关系和资金反证均应视为 **未知/不可用**，不应继续用于板块判断。

因此，本次自查不为 `257.84亿元` 寻找事后解释，也不把其他供应商的数据硬套为修正值；正确处置是撤销该数值的“已验证三日累计净额”含义，并启动同口径重算。

## 二、当前报告的资金净额口径

### 2.1 报告现在实际采用的口径

从两份报告载荷可确认：

- 供应商：同花顺。
- 行业分类：同花顺行业，半导体代码为 `881121`。
- 供应商窗口：3日、5日、10日、20日。
- 金额单位：亿元人民币。
- 报告字段：`vendor_net_3`、`vendor_net_5`、`vendor_net_10`、`vendor_net_20`。
- 报告没有用自己的逐日资金序列计算上述窗口，而是读取供应商各周期排行页的快照值。

同花顺行业资金页为各窗口展示“流入资金、流出资金、净额”；[同花顺行业资金流页面](https://data.10jqka.com.cn/funds/hyzjl/)和 [AKShare 对该页面的接口说明](https://github.com/akfamily/akshare/blob/main/docs/data/stock/stock.md)均列出了即时、3日、5日、10日和20日排行及其字段。

当前报告中净额和占比的计算形式为：

```text
供应商净额_N = 供应商流入资金_N - 供应商流出资金_N

报告净额占比_N = 供应商净额_N
                 ÷（供应商流入资金_N + 供应商流出资金_N）
```

分母为供应商所划分的“流入 + 流出”，不是行业总成交额，也不是行业市值。正值表示该供应商口径下流入大于流出，负值表示流出大于流入。

以半导体 8 月 31 日三日值为例，供应商原始行对应流入 `809.04亿元`、流出 `812.72亿元`：

```text
净额 = 809.04 - 812.72 = -3.68亿元
占比 = -3.68 ÷ (809.04 + 812.72) = -0.2269% ≈ -0.2%
```

### 2.2 “意图口径”与“目前可证明口径”必须分开

| 项目 | 原报告暗示/预期含义 | 自查后目前只能证明的含义 |
|---|---|---|
| N日净额 | 截止报告日最近 N 个交易日的净额累计 | 生成时从供应商 N 日排行入口取得的快照值 |
| 窗口截止日 | 与报告 `cutoff` 相同 | 报告载荷没有供应商业务日期，无法证明 |
| 累计方法 | 同一逐日序列求和 | 没有保存逐日分解，无法复算 |
| 行业成分 | 同一版本行业成分 | 只有分类版本名，没有每个窗口的成分版本或数量快照 |
| 数据修订 | 历史值可追溯 | 没有保存原始响应、抓取时间和响应哈希，无法区分修订、缓存或采集错位 |

因此，在修复前，更准确的字段名称应是“供应商 N 日排行快照净额（窗口未核验）”，而不是不带限定的“N 日净额”。

### 2.3 该指标不是什么

该资金流向指标不是以下指标：

- 不是北向资金净买入。
- 不是融资融券净买入。
- 不是 ETF 份额申赎资金流。
- 不是银行存款或真实现金进入/离开股市的会计口径。
- 不能直接等同于机构席位的真实净交易。
- 不能与其他供应商的“主力资金”金额直接拼接或替换。

资金流向通常是依据逐笔成交方向、成交规模等规则形成的代理指标。不同供应商的订单分类、主动买卖判断、行业成分和盘中/盘后修订均可能不同。因此，跨供应商数据只能用于异常方向复核，不能在没有口径对齐时进行金额对账。

### 2.4 “价资关系”也不是当日涨跌与当日资金的关系

报告字段字典将 `daily_price_flow_relation` 定义为：

```text
5日价格收益率 + 3日/5日供应商资金净额符号
```

所以 9 月 1 日当日指数下跌，并不直接否定报告中的 `ALIGNED_POSITIVE`；该标签比较的是五日价格窗口与三日/五日资金窗口。现有名称中的 `daily` 容易让人误解为“当日价资关系”，后续应改名为“5日价格-短窗资金关系”，并展示实际窗口起止日。

不过，由于三日和五日资金数据本身未通过本次质量检查，9 月 1 日的 `ALIGNED_POSITIVE` 同样不能继续采用。

“30日量价资金压力代理”是另一个基于量价特征和历史校准的代理量，不是上述供应商净额，也不能为异常的供应商窗口数据背书。

## 三、半导体样本复现

### 3.1 两份已发布报告中的原始值

| 报告日 | 3日净额/占比 | 5日净额/占比 | 10日净额/占比 | 20日净额/占比 | 资金阶段 | 价资关系 | 资金反证 |
|---|---:|---:|---:|---:|---|---|---|
| 2026-08-31 | -3.68亿 / -0.2% | -150.44亿 / -7.0% | 322.38亿 / 10.3% | -110.83亿 / -4.3% | MIXED_FLOW | PRICE_UP_FLOW_OUT | NONE |
| 2026-09-01 | 257.84亿 / 11.5% | 48.42亿 / 2.8% | -152.60亿 / -5.0% | 219.35亿 / 8.3% | MIXED_FLOW | ALIGNED_POSITIVE | EARLY_FLOW_REVERSAL |

证据位置：

- 8 月 31 日报告：Git 提交 [`be77476`](https://github.com/ZJU-whw/yiziemei/blob/be7747652a520c8015214dc3436b90daf14e9cda/A股板块潜力与龙头图形股研究报告_20260831.html)。
- 9 月 1 日报告：Git 提交 [`3ab8284`](https://github.com/ZJU-whw/yiziemei/blob/3ab8284bf05aab25c6af5796d4ffc04dff890d23/A股板块潜力与龙头图形股研究报告_20260901.html)。

两份报告载荷分别声明：

```text
cutoff = 2026-08-31 / 2026-09-01
provider = 同花顺
provider_windows = [3, 5, 10, 20]
```

### 3.2 用户提出的滚动窗口推导是正确的

如果三日净额确实是同口径逐日净额的最近三个交易日之和，则：

```text
F3(2026-08-31) = F(08-27) + F(08-28) + F(08-31)
F3(2026-09-01) = F(08-28) + F(08-31) + F(09-01)

所以：
F3(09-01) - F3(08-31) = F(09-01) - F(08-27)
```

代入报告值：

```text
257.84 - (-3.68) = 261.52亿元

即：F(09-01) - F(08-27) = +261.52亿元
```

这意味着，若报告口径成立，9 月 1 日单日净额必须比 8 月 27 日单日净额高 `261.52亿元`。这不是单纯说明“9 月 1 日大幅流入、8 月 27 日大幅流出”；两天可以同时流入或同时流出，但二者必须满足上述差值。

公开盘面资料则显示相反方向：8 月 27 日电子、半导体、通信为主要净流入方向，而 9 月 1 日电子、半导体为主要净流出方向。[8 月 27 日盘中资金方向](https://finance.sina.com.cn/roll/2026-08-27/doc-inipttav5428592.shtml)、[9 月 1 日盘中资金方向](https://finance.sina.com.cn/roll/2026-09-01/doc-iniqiatv8190263.shtml)。这些资料的供应商及分类口径未必与本报告一致，不能用来替换报告金额，但足以构成方向性异常警报。

结论：用户从窗口差值提出的疑问成立；价格涨跌本身不是严格证明，但报告数据需要提供逐日分解才能自证，目前做不到。

## 四、全量自查发现

### 4.1 不是半导体单点异常

对 Git 历史中的报告载荷按 `industry_code` 对齐后，得到以下结果：

| 检查 | 结果 | 正常滚动窗口下的判断 |
|---|---:|---|
| 2026-08-26 与 2026-08-27 的 3/5/10/20日净额 | 90个行业 × 4窗口，`360/360` 完全相同 | 新增一个完整交易日后不应全市场逐项完全相同 |
| 同上各窗口净额占比 | `360/360` 完全相同 | 金额和占比同时全量相同，说明快照被复用或时点未切换 |
| 2026-08-31 的5日净额 vs 2026-08-27的3日净额 | `90/90` 完全相同 | 五日窗口比三日窗口多两个交易日，不应全行业完全相同 |
| 同上净额占比 | `90/90` 完全相同 | 不可能由个别行业巧合解释 |
| 2026-09-01 的5日净额 vs 2026-08-28的3日净额 | `90/90` 完全相同 | 再次出现系统性窗口错位 |
| 同上净额占比 | `90/90` 完全相同 | 进一步排除四舍五入偶合 |

半导体只是最直观的例子：

```text
2026-08-27 3日净额 = -150.44亿
2026-08-31 5日净额 = -150.44亿

2026-08-28 3日净额 =  48.42亿
2026-09-01 5日净额 =  48.42亿
```

这两组相等关系同时在全部 90 个行业、净额和占比两个字段上成立，属于系统性数据时点/窗口异常。

### 4.2 回填报告存在“报告截止日”与“供应商采集时点”混淆

Git 记录显示：

- 标注截止日为 8 月 26 日的报告，最终版本提交于 8 月 28 日 `00:40 +08:00`。
- 标注截止日为 8 月 27 日的报告，提交于 8 月 28 日 `05:21 +08:00`。
- 两份报告的全部供应商资金窗口完全相同，但价格数据存储哈希不同。

同花顺公开排行入口主要提供当前快照，报告又没有记录各窗口供应商业务日期。若在 8 月 27 日收盘后回填 8 月 26 日报告，仅修改报告 `cutoff` 而读取当时的当前快照，就会造成“价格截止 8 月 26 日、资金截止 8 月 27 日”的穿越污染。

由于历史 HTTP 原始响应和生成代码没有随报告入库，本次无法证明该污染发生在供应商、缓存层还是采集程序；但报告把未验证的供应商时点写成确定的 `cutoff`，属于确定的发布问题。

### 4.3 对供应商公开页面的当场复核

2026-09-02 10:08（盘中）读取同花顺公开行业资金排行，半导体返回：

| 入口 | 阶段涨跌幅 | 流入资金 | 流出资金 | 净额 |
|---|---:|---:|---:|---:|
| 3日排行 | -3.04% | 923.95亿 | 1,074.02亿 | -150.08亿 |
| 5日排行 | +1.61% | 809.04亿 | 812.72亿 | -3.68亿 |
| 10日排行 | -7.51% | 1,437.58亿 | 1,764.49亿 | -326.91亿 |

其中，9 月 2 日盘中五日入口的 `809.04 / 812.72 / -3.68`，恰好重现 8 月 31 日报告三日值及其占比分母：

```text
-3.68 ÷ (809.04 + 812.72) = -0.2269%
```

同时，五日入口的“流入 + 流出”小于三日入口。如果这些字段是同一截止时点下的累计金额，这是不可能的。

该复核说明公开供应商入口本身可能存在跨窗口刷新、缓存或数据定义问题；也说明报告不能再把各入口值无条件当作同一截止日的嵌套累计窗口。由于此次查询发生在盘中，表中数值只用于验证接口行为，不作为 9 月 1 日的修正值。

### 4.4 发布审计没有覆盖时间序列正确性

8 月 28 日、8 月 31 日和 9 月 1 日报告均声明：

```text
structural_audit_status = PASS
semantic_audit_status   = PASS
render_audit_status     = PASS
publish_status          = PASS
```

但目前的“语义审计”没有检查：

- 供应商业务日期是否等于报告截止日。
- N 日窗口究竟包含哪些交易日。
- 净额是否等于流入减流出。
- 净额占比是否可由原始字段复算。
- 相邻日滚动窗口恒等式是否成立。
- 长窗口流入/流出累计额是否不小于短窗口。
- 不同日期或不同窗口是否出现异常的全量精确复制。
- 回填历史报告时是否使用了未来快照。

因此，现有 `PASS` 只能理解为“HTML 和字段合同可发布”，不能理解为“资金数据已通过业务正确性验证”。

## 五、根因判断

### 5.1 已确认事实

1. 报告依赖四个供应商周期快照，没有以同一逐日序列自主计算滚动窗口。
2. 报告载荷没有保存供应商每个窗口的业务日期、抓取时间、原始流入/流出、响应标识和响应哈希。
3. 报告载荷没有保存三日窗口的逐日分解，无法回答 `08-27` 和 `09-01` 各自贡献多少。
4. 全行业存在系统性重复/错位，正常市场行为无法解释。
5. 当前审计没有拦截上述异常，仍发布了派生标签。

### 5.2 尚不能唯一确定的责任点

目前不能仅凭最终 HTML 唯一判断以下哪一层是首要根因：

- 同花顺周期排行后端的数据或刷新异常。
- CDN/接口缓存键未正确区分窗口或业务日期。
- 采集器将不同窗口、不同日期的响应错配。
- 历史回填时使用了当前快照。
- 中间存储按位置或最近可用值进行了错误填充。

直接查询公开页面时也观察到窗口异常，增加了供应商/缓存层异常的可能性；但由于生成时的原始响应未归档，不能免除采集和发布链路的校验责任，也不能武断把全部责任归给供应商。

### 5.3 根因定位所缺的证据

后续必须补取或新增：

- 报告生成代码及其版本号。
- 8 月 27 日、28日、31日和 9 月 1 日每个窗口的原始 HTTP 响应。
- 响应头、请求参数、抓取时间、业务日期和内容哈希。
- 采集后的标准化中间表。
- 窗口合并键、去重规则、缺失值处理和缓存策略。
- 同花顺行业成分版本及每个行业的公司数量。

## 六、影响范围

以下字段直接或间接受影响：

| 字段/结果 | 影响 |
|---|---|
| `vendor_net_3/5/10/20` | 原始窗口值不可解释为已验证累计净额 |
| `vendor_net_share_3/5/10/20` | 分子和窗口时点异常，比例不可用 |
| `flow_component_percentile` | 由异常窗口值形成的横截面分位不可用 |
| `flow_regime_state` | 资金阶段可能误判 |
| `price_flow_relation` | 价资关系可能误判 |
| `flow_contradiction` | `EARLY_FLOW_REVERSAL` 等反证可能为伪信号 |
| 四维确认中的资金维度 | 应降级为 `UNKNOWN`，不能作为 PASS/FAIL |
| 板块总分和排名 | 若包含资金分量，应重算并标注版本变化 |
| 风险监控和状态迁移 | 由资金标签触发的变化应撤销或重算 |

`money_pressure_30` 属于独立的量价代理，未发现它直接读取上述供应商净额；本次不能据此认定该字段错误。但它也不能替代供应商资金窗口，更不能证明资金窗口正确。

## 七、立即处置建议

### 7.1 发布侧止损

在完成同口径重算前：

1. 将 2026-08-26 至 2026-09-01 报告的资金轮动频道标记为 `DATA_QUALITY_FAIL`。
2. 将 `vendor_net_*`、`flow_regime_state`、`price_flow_relation`、`flow_contradiction` 显示为“待重算/未知”。
3. 资金维度不得参与板块确认、总分、排名、风险迁移或“早期反转”判断。
4. 报告顶部增加勘误提示，避免读者继续引用已发布值。
5. 在修复完成前，不再发布没有逐日分解和窗口审计的 N 日资金值。

### 7.2 本次半导体数值的处置

| 日期 | 原发布3日净额 | 自查后状态 | 修正值 |
|---|---:|---|---:|
| 2026-08-31 | -3.68亿 | 撤销“已验证三日累计”含义 | 待同口径逐日数据重算 |
| 2026-09-01 | 257.84亿 | 撤销“已验证三日累计”含义 | 待同口径逐日数据重算 |

不能用新闻中的电子行业、申万行业、概念板块、ETF申赎或其他供应商主力资金数值直接填补。行业分类和算法口径不同，强行替换会制造新的错误。

## 八、永久修复方案

### 8.1 以逐日事实表为唯一计算来源

建立行业逐日资金事实表，每条记录至少包含：

```text
trade_date
industry_code
industry_name
classification_version
constituent_count
provider
provider_metric_version
inflow_cny
outflow_cny
net_cny
source_business_date
fetched_at
request_url_or_dataset_id
raw_response_sha256
quality_status
```

按报告截止日取得同一供应商、同一分类版本的逐日记录后，自主计算：

```text
net_N(t) = Σ daily_net(d)
           d ∈ 截止 t 的最近 N 个交易日

inflow_N(t)  = Σ daily_inflow(d)
outflow_N(t) = Σ daily_outflow(d)

net_share_N(t) = net_N(t) ÷ (inflow_N(t) + outflow_N(t))
```

供应商 N 日排行只能作为交叉核验数据，不再作为唯一事实源。可选的历史日频接口必须明确授权和口径，例如 [Tushare 同花顺行业资金流向日频接口说明](https://tushare.pro/document/2?doc_id=343)；若更换数据源，必须建立新版本，不能与旧值无痕混用。

### 8.2 强制展示窗口日期

报告不再只显示“3日”，而应显示：

```text
3日（2026-08-28、2026-08-31、2026-09-01）
```

每个窗口同时显示：

- 截止交易日。
- 实际覆盖交易日数。
- 起止日期及交易日列表。
- 数据源和指标版本。
- 完整/部分/未知状态。

### 8.3 新增发布阻断规则

以下任一规则失败，资金频道不得发布确定数值：

#### 单行算术校验

```text
abs(net - (inflow - outflow)) <= 金额精度容差
```

#### 相邻交易日滚动恒等式

```text
net_N(t) - net_N(t-1)
= daily_net(t) - daily_net(t-N)
```

#### 嵌套窗口累计校验

同一截止日、同一分类版本下：

```text
inflow_20 >= inflow_10 >= inflow_5 >= inflow_3 >= 0
outflow_20 >= outflow_10 >= outflow_5 >= outflow_3 >= 0
```

#### 时点校验

```text
source_business_date == report_cutoff
max(trade_date in window) == report_cutoff
```

历史回填只能使用该历史日及以前已存在的原始记录，禁止读取当前排行快照后回写旧 `cutoff`。

#### 异常重复校验

- 相邻交易日同一窗口若超过约定比例的行业金额和占比完全相同，阻断发布。
- 不同窗口与历史其他窗口若出现大面积逐项完全相同，阻断发布。
- 本次出现的 `90/90` 精确复制必须被自动测试捕获。

#### 完整性校验

- 缺少任一交易日，不得用零或最近值填充。
- 数据不足时输出 `UNKNOWN_INCOMPLETE_WINDOW`。
- 行业成分发生变化时记录版本，并决定是否重算历史或断开可比性。

### 8.4 原始证据归档

每次报告发布应保存：

```text
raw/provider/YYYYMMDD/window_3.*
raw/provider/YYYYMMDD/window_5.*
raw/provider/YYYYMMDD/window_10.*
raw/provider/YYYYMMDD/window_20.*
normalized/industry_daily_flow_YYYYMMDD.*
audit/flow_reconciliation_YYYYMMDD.json
```

归档应带内容哈希、抓取时间和生成代码提交号，使未来可以区分：供应商回溯修订、缓存异常、采集错误和报告计算错误。

### 8.5 派生标签的降级传播

资金原始值一旦为未知，派生字段必须按依赖关系传播未知：

```text
vendor window invalid
  → flow_component_percentile = UNKNOWN
  → flow_regime_state = UNKNOWN
  → price_flow_relation = UNKNOWN
  → flow_contradiction = UNKNOWN
  → flow dimension = UNKNOWN
  → total score/rank excludes flow or whole result marked incomplete
```

不得把未知默认为零，也不得沿用前一日标签。

## 九、修复验收标准

资金频道恢复发布前，至少满足：

- [ ] 明确供应商资金流入/流出的定义、订单分类和盘中/盘后修订规则。
- [ ] 保存行业逐日流入、流出、净额和业务日期。
- [ ] 3/5/10/20 日值全部由同一逐日事实表计算。
- [ ] 半导体 8 月 27 日至 9 月 1 日逐日分解可复算两份报告。
- [ ] 全部 90 个行业通过净额、占比、窗口和时点校验。
- [ ] 相邻日滚动恒等式通过。
- [ ] 回填报告不存在未来数据污染。
- [ ] 原始响应、标准化数据、审计结果和代码版本可追溯。
- [ ] 资金缺失会降级为未知并阻断派生标签。
- [ ] 用至少一个独立来源做方向性抽查，差异有书面解释。
- [ ] 修复后重发报告带版本号和勘误说明。

## 十、对本次疑问的直接回答

### 1. 9月1日下跌但三日净流入257.84亿元，理论上合理吗？

仅从“价格下跌、资金净流入”看，理论上可能；价格与资金流向代理并非严格同号，三日值也不是单日值。

但结合这两份报告的窗口算术、全行业精确复制、公开页面窗口异常和外部方向复核，本次 `257.84亿元` **不能被接受为已验证的最近三交易日累计净流入**。

### 2. 用户关于8月27日和9月1日的推导对吗？

对。若两份三日值同口径且都是真正滚动累计，则必有：

```text
9月1日单日净额 - 8月27日单日净额 = 261.52亿元
```

当前报告没有逐日数据证明这一关系，且可获得的方向证据与之冲突。

### 3. 资金净额到底是什么口径？

当前实现是：同花顺行业资金排行各周期入口的“流入资金减流出资金”，单位亿元；报告占比为“净额除以流入与流出之和”。它是供应商资金流向代理，不是北向、融资、ETF申赎或真实会计资金净进入。

更重要的是，当前实现读取供应商周期快照，而不是由报告自己的逐日序列求和。因此，在完成修复前，不能再把它表述为经过验证的“N 日累计净额”。

### 4. 自查后的正式处置是什么？

将 8 月 31 日和 9 月 1 日半导体资金窗口值列为待重算；将受其影响的资金阶段、价资关系和资金反证撤销为未知。只有取得同一口径逐日原始数据、通过上述窗口校验后，才能给出修正值和恢复相关标签。

---

本报告是数据质量自查和勘误依据，不构成投资建议。自查结论的重点不是判断半导体后续涨跌，而是确保资金轮动频道的每一个窗口值都能从带日期的逐日事实记录复算并审计。
