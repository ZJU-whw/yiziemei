# 《A股板块潜力与龙头图形股研究报告_20260828.html》优化复验评审

> 评审日期：2026-08-31（Asia/Shanghai）  
> 优化基准：[result8.md](./result8.md)  
> 前次问题基线：[result6.md](./result6.md)  
> 被评审远端版本：`65522a21aec84980b17d2f8279d68b3b2b3344f0`（2026-08-31 19:35:12 +08:00）  
> 远端最新 `main`：`66a13c090d42c96919e832bb85f2355b8d08ff73`  
> 报告 Git blob：`de45aaf60e240cd9cacc3313953e180b177cd9f0`  
> 报告 SHA-256：`f56aa2f0e6191523cf96b41a8a7782549c3ca0e459f7af2b90fbeb02879f6cc9`

## 一、验收结论

**本次复验结论：不通过。**

新版有实质进步：8 月 27 日已改标为展示子集，上一版 462 条无法证明的图形“新增”已被抑制；周内图形事件由 571 条收敛为 19 条可见交集内的状态/层级变化；下周观察计划由 10 条缩为 4 条，4/4 均满足入选条件且 0/4 触发观察退出；原 15 条 L3 价位冲突在新页面字段中均改为 `null + UNKNOWN`。日报行动层、状态机合同和生成/发布时间字段也已开始落地。

但 `result8.md` 规定的 P0 完成定义仍未满足。当前至少存在以下阻断项：

1. 图形快照审计仍混用“来源声明数量、实际嵌入数量和页面展示数量”，且当前 HTML 声称嵌入 493 条，实际可审计数组只有 51 条；
2. 要求新增的六个价资口径字段在载荷中全部缺失，风险背离数据仍与日报价资关系冲突；
3. 实际使用的多个枚举仍不在 glossary 中，页面仍会裸显英文代码；
4. `semantic_audit_status=PASS`、`publish_status=PASS` 没有拦截上述语义错误；`render_audit_status=PASS` 仅基于离线模板合同，不是真实浏览器验收；
5. 日报 B/C 池、资金榜、龙头一致性、风险分层和字段字典仍未按 P1 口径闭合；
6. 本期实际生成时间晚于发布窗口 43 小时 24 分，虽然已如实标为 `LATE_PUBLICATION`，但发布时效本身仍未达标。

因此，本报告仍只能作为 **`RESEARCH_ONLY / WAIT` 的研究试运行产物**。在修正语义审计前，不应把其自报的 `publish_status=PASS` 作为可发布证明，也不应把图形快照哈希、风险分组或价资背离标签用于自动化下游决策。

## 二、版本与结构核验

本次直接验收远端 Git 对象，没有合并远端同时发生的无关文件删除。结构事实如下：

| 核验项 | 实际结果 | 结论 |
|---|---:|---|
| 日报/周报频道 | 8 / 7，共 15 个标签 | 通过 |
| HTML 静态 DOM 合同 | 15 个按钮与 15 个 section 一一对应；59 个 ID 唯一；无缺失的 `getElementById` 目标 | 通过 |
| 当前行业 | 90 条、90 个唯一代码、排名 1～90 | 通过 |
| 周内行业路径 | 90 条，覆盖 5 个交易日 | 通过 |
| 周五迁移 | 日报 80 条、周报保留 80 条 | 数量通过，字段一致性不通过 |
| 市场闸门 | `FAIL=31`、`UNKNOWN=59`、`PASS=0` | 正确失败关闭 |
| 正式个股研究 | 0 条 | 正确空状态，未用 L2/L3 补位 |
| 图形页面数组 | 51 条；`FORMING=44`、`TRIGGERED=7` | 数量可读，枚举未闭合 |
| 周内图形事件 | 19 条：状态变化 9、层级升级 10 | 相比上一版明显改善 |
| 下周观察计划 | 4 条；入选 4、退出触发 0 | 通过 |
| 字段字典 / glossary | 15 / 176 条 | 字段字典严重不足 |
| 自报发布审计 | 结构、语义、渲染、发布均为 `PASS` | 与实测矛盾 |

## 三、P0 阻断项复验

| P0 项 | 结论 | 复验结果 |
|---|---|---|
| P0-01 图形跨日快照可比性 | **部分修复，未关闭** | 已纠正 8 月 27 日范围并禁止伪新增；但数量与嵌入范围仍不闭合，见 3.1。 |
| P0-02 下周计划分阶段条件 | **通过** | 4/4 `watch_entry_met=true`，0/4 `watch_exit_triggered=true`；观察退出、正式升级、确认后失效已分栏。 |
| P0-03 触发前撤销与触发后失效 | **基本修复，兼容口径未关闭** | 新字段修复了原 15 条误判；旧字段仍保留冲突值且未标记废弃，见 3.2。 |
| P0-04 S0～S6 状态机 | **部分通过/材料不足** | 合同、阈值、质量顺序和旧映射已嵌入；缺需求所有者批准及回归证明，合法迁移表又允许任意不同状态互转。 |
| P0-05 枚举与字段口径闭合 | **不通过** | 六个要求字段均未落入载荷，价资背离冲突未消除，至少 13 个实际代码缺 glossary。 |
| P0-06 可发布语义审计 | **不通过** | 明显语义问题未阻断发布，且无负例测试、真实浏览器日志或截图。 |

### 3.1 图形快照审计仍不闭合

已确认的正向修复：

- 8 月 24～27 日均改为 `DISPLAY_SUBSET`，8 月 28 日为 `FULL_SNAPSHOT`；
- 8 月 27 日→28 日不再输出 `PATTERN_ADDED/PATTERN_REMOVED`；
- 19 条事件均带行业、股票、图形工作流、前后快照哈希，且无同日同键重复；
- 四段审计的 `changed_count` 合计 19，与事件数组一致。

仍未关闭的事实：

1. `pattern_research_candidates` 实际只有 51 条，`payload_counts.pattern_research_candidates` 也为 51；但 `audit_counts.pattern_research_candidate_count=493`，当前 manifest 又声明 `pattern_embedded_count=493`。HTML 中不存在另一份可审计的 493 条完整记录数组，因而“已嵌入 493 条”无法成立或复核。
2. 比较审计的数量字段混用了不同口径。例如 8 月 27 日→28 日写有 `prior_count=380`、`current_count=493`、`comparable_count=31`、`excluded_count=489`。若前两项是集合数量，排除数应为 `380 + 493 - 2×31 = 811`；489 实际对应的是上一日嵌入 58 条与本日 493 条的计算：`58 + 493 - 2×31`。审计对象没有同时公开 `prior_embedded_count/current_embedded_count`，字段本身不能闭合。
3. 同类混用还发生在 8 月 25 日→26 日（声明 10/451，排除数却按 10/60 计算）和 8 月 26 日→27 日（声明 451/380，排除数却按 60/58 计算）。
4. 当前完整快照记录未随 HTML 交付，`pattern_snapshot_sha256` 无法由报告载荷独立重算。

所以，上一版的伪新增已消除，但 P0-01 的“范围、数量、哈希可闭合”仍未完成。

### 3.2 原 15 条价位冲突已修正展示，但旧字段未废弃

逐条回归中金岭南、永杰新材、比音勒芬、浙富控股、赛恩斯、艾布鲁、华旺科技、浙江东日、南方航空、中国东航、上海电影、光线传媒、捷成股份、华策影视、爱美客后，15/15 均满足：

- `pre_trigger_cancel_level=null`、状态 `UNKNOWN`；
- `post_trigger_invalidation_level=null`、状态 `UNKNOWN`；
- `pattern_status=FORMING`；
- `unknown_fields` 明确列出两个未知价位；
- D5、D7、W5 已改用新字段，不再把触发位当作页面失效位。

此外，51 条展示记录中有 16 条有效触发后失效位，0 条出现“失效位不低于触发位”。这部分修复有效。

但兼容字段 `invalidation_level` 和 `invalidation_condition=CLOSE_LT_INVALIDATION_LEVEL` 仍在载荷中；51 条中仍有 21 条 `invalidation_level == trigger_level`，其中包含上述 15 条。报告没有 `deprecated` 标记，字段字典也未说明旧字段废弃。页面用户已不再被误导，但仍可能误导继续消费旧字段的下游程序，未完全达到 result8 要求的“双写后标废弃”。

### 3.3 价资字段与枚举闭合仍失败

result8 要求新增并清晰区分以下六个字段。新报告中的实际键出现次数均为 0：

| 要求字段 | 实际出现次数 |
|---|---:|
| `daily_price_flow_relation` | 0 |
| `daily_price_flow_window` | 0 |
| `risk_price_flow_divergence` | 0 |
| `risk_price_flow_window` | 0 |
| `weekly_price_flow_tags` | 0 |
| `weekly_price_flow_rule` | 0 |

载荷仍使用旧的 `price_flow_relation`（90 次）和 `weekly_flow_tags`（132 次）。字段字典虽然列出了 `daily_price_flow_relation`、`risk_price_flow_divergence` 和 `weekly_price_flow_tags`，但这些字段并不存在于数据中，形成“字典有字段、载荷无字段”的幽灵定义。

语义冲突也未消失：

- 30 个行业的 `risk_flags` 含 `PRICE_FLOW_DIVERGENCE`；
- 只有 6 个行业的 `price_flow_relation` 属于价资背离状态；
- 两组交集仅 2 个；另有 4 个价强资弱行业的 `risk_flags=NONE`；
- 新字段字典却把日报关系和风险背离都声明为同一 `5D_PRICE_WITH_3D_5D_FLOW` 窗口，并称风险背离由日报关系直接派生。

这不是可由“窗口不同”解释的差异，而是字段定义与实际数据相互矛盾。

对实际载荷与页面 `label()/labelPipe()` 消费值做覆盖检查，至少以下实际代码不在 glossary：

`ALL_MARKET_STATES`、`COMMON_VISIBLE_ONLY`、`COVERAGE_NOT_COMPLETE`、`DISPLAY_SUBSET`、`ELIGIBLE_MAINLINE_WITHOUT_EXIT_SIGNAL`、`FORMAL_DISPLAY_ONLY`、`FULL_SNAPSHOT`、`LOW_CONFIDENCE`、`MARKET_STATE:ROTATION`、`NATIVE_PIPELINE`、`PUBLISHED_HTML_GIT_BLOB_NORMALIZED`、`RADAR_CONFIRMED`、`TRIGGERED`。

其中 `RADAR_CONFIRMED` 仍真实存在于周内路径，`LOW_CONFIDENCE` 仍真实存在于风险字段，正是前次评审点名的缺项。说明文字还写“样本不足显示 `INSUFFICIENT_SAMPLE`”，实际正式 glossary 仍只有 `LOW_SAMPLE`。枚举统一未完成。

页面还有已具 glossary 但仍直接用 `esc()` 裸显的代码，例如风险分组 `NEW_OR_WORSENED/PERSISTENT_RISK/DATA_UNKNOWN`、发布状态 `LATE_PUBLICATION`，以及若干 JSON 化的条件评价和阈值键。

### 3.4 `PASS` 审计存在假阳性

报告自报：

```text
structural_audit_status = PASS
semantic_audit_status   = PASS
render_audit_status     = PASS
publish_status          = PASS
blocking_errors         = []
warnings                = []
render_audit_basis      = OFFLINE_HTML_TEMPLATE_CONTRACT
```

但 P0-01、P0-05 以及下文的风险分层、字段字典和迁移字段一致性均能在同一 HTML 内直接复现。按 result8 的发布规则，`semantic_audit_status` 和 `publish_status` 至少应为 `FAIL`，不应为空错误列表。

静态 DOM 合同本身通过，但 `OFFLINE_HTML_TEMPLATE_CONTRACT` 不能替代“15 个标签经真实浏览器渲染”的要求。远端仓库没有提交浏览器截图、控制台日志或渲染测试产物，也没有提交“快照范围不一致、Top10 自失效、价位冲突、未知枚举”四类负例必须构建失败的证据。

另一个自动检查也未通过：`daily_transition_ledger` 与 `friday_daily_transitions` 虽然都是 80 条、行业代码及共有字段值一致，但前者每条仅 11 个字段，后者每条 39 个字段，不是 result8 要求的“逐字段一致”。

## 四、P1 日报与周报逐项验收

### 4.1 日报 P1-01～P1-08

| 项目 | 结论 | 核验结果 |
|---|---|---|
| P1-01 30 秒行动摘要 | **部分通过** | 有 5 个重点板块、四维差值、退出和复核；但页面未显示 `daily_radar_distribution`，重点卡也没有渲染已有的 `daily_change_reasons`，全局“市场是否允许”仍缺闸门分布/明确结论。 |
| P1-02 B/C 早期池 | **不通过** | 42 条确为 B/C，但要求的 `return_5/10/20`、`volume_ratio`、`resistance_level`、`distance_to_resistance_pct` 均不存在；只有 `DAILY/UNCHANGED`，没有入池、升级、降级、退出账本。 |
| P1-03 日资金榜 | **部分通过** | 只有加速 Top10、撤退 Top10；缺流出转流入、流入转流出、价强资弱、价弱资强。页面也未完整展示榜单阈值与逐窗变化。 |
| P1-04 龙头日变化 | **部分通过** | 有 27 条 8 月 28 日新增/退出/降级；但使用 `sector_confirmation_alignment` 而非约定字段，缺 `alignment_reason`，页面也未显示已有的变更原因。 |
| P1-05 图形主表关键字段 | **基本通过** | 距触发、触发状态、前撤销、后失效、风险和未知均已展示；受 `TRIGGERED` 缺 glossary 和旧字段未废弃影响，尚非完全闭合。 |
| P1-06 四维量化缺口 | **通过** | 90 行业均有四维状态、当前值、阈值、差值、连续性、反证和复核时间；日报、早期池、下周计划的重合行业逐字段一致。 |
| P1-07 风险分层 | **不通过** | 分组数量闭合为 0/31/59，但默认大表仍因 `market_gate_state != PASS` 展示 90/90；且 8 月 28 日有 5 个行业新进入 S5/S6，`NEW_OR_WORSENED` 却为 0。 |
| P1-08 字段字典 | **不通过** | 仅 15 条，未覆盖资金四窗口、扩散、图形状态、距离、风险、历史概率等大量页面字段；还存在字典字段与载荷字段不一致。 |

风险分层的具体反例是：化学纤维、黑色家电、煤炭开采加工、种植业与林业、白酒均在 8 月 28 日由非风险状态进入 S5/S6，却全部没有进入 `NEW_OR_WORSENED`。这说明当前分组只是把 31 个闸门 FAIL 归为持续风险、59 个闸门 UNKNOWN 归为数据未知，并未实现要求的日风险变化语义。

### 4.2 周报 P1-09～P1-13

| 项目 | 结论 | 核验结果 |
|---|---|---|
| P1-09 风格/板块轮动摘要 | **基本通过** | 输出 5 条，均带日期、行业代码和证据字段；表达仍以结构卡片为主，证据字段名裸显英文。 |
| P1-10 主线入选规则 | **通过** | formed 0、forming 10、potential 5；规则细节、阈值、退出原因和证据变化均存在，空池未补位。 |
| P1-11 可信典型案例 | **通过空状态** | 本周 8 月 25 日→26 日共同可见对象为 0，没有贯穿全周的完整可比序列；案例数组为空并明确说明，没有从不可比记录硬选。 |
| P1-12 下周计划表达 | **通过** | 4 条真实对象、不补足；条件分阶段、量化缺口、反证、复核、动作和生成时评价均存在。 |
| P1-13 生成与发布审计 | **部分通过** | 元数据和 `LATE_PUBLICATION` 已实现；实际生成于 8 月 31 日 19:23，距 8 月 29 日 23:59 窗口结束晚 43 小时 24 分，故本期发布节奏不通过。 |

## 五、result8 十六项自动检查复核

| # | 自动检查 | 结论 |
|---:|---|---|
| 1 | 快照范围和版本可比；不可比时禁止新增/移除 | **部分通过**：已禁止伪新增，但完整/嵌入数量与哈希仍不闭合。 |
| 2 | 图形事件计数与数组、前后集合闭合 | **事件自身通过**：19 条无重复且与 changed 合计一致；全量集合仍不可复核。 |
| 3 | Top10/观察计划入选时退出条件不得触发 | **通过**：4/4 入选、0/4 退出。 |
| 4 | 撤销位、触发位、失效位关系合法 | **新字段通过**：16 个有效后失效位均低于触发位；旧字段未废弃。 |
| 5 | 状态、风险、候选池去留一致 | **部分通过**：当前无正式补位；缺失 INVALIDATED/正式确认历史样例，旧字段仍有歧义。 |
| 6 | 所有允许和实际枚举均有 glossary | **不通过**：至少 13 个实际代码缺失。 |
| 7 | 所有页面字段均有 field dictionary | **不通过**：仅 15 条且存在幽灵字段。 |
| 8 | 生命周期迁移符合版本化状态机 | **部分通过**：周五质量顺序与升级/降级原因无冲突；批准材料缺失，合法迁移表无约束力。 |
| 9 | 日报与周报四维值逐字段一致 | **通过**。 |
| 10 | 日榜用日变化、周榜用周变化 | **通过当前两类日榜**；其余四类日榜尚未实现。 |
| 11 | 风险分层计数与全量表闭合 | **数量通过、语义不通过**：0/31/59 虽合计 90，但新增恶化分类错误且默认仍平铺 90 行。 |
| 12 | 周五日报迁移与周报保留记录逐字段一致 | **不通过**：11 字段与 39 字段。 |
| 13 | UNKNOWN/PARTIAL 不得通过市场闸门 | **通过**：PASS 为 0。 |
| 14 | 正式候选不得由 L2/L3 补位 | **通过**：正式候选为 0。 |
| 15 | 15 标签真实浏览器渲染 | **未举证/不通过**：只有离线模板合同，无浏览器日志或截图。 |
| 16 | 任一结构、语义、渲染 FAIL 时禁止发布 PASS | **不通过**：报告仍自报 `publish_status=PASS`。 |

## 六、复验材料完整性

result8 要求下一轮一次性提交 12 类材料。当前远端新增提交只更新了目标 HTML，仓库中没有相应测试或截图产物：

| 材料 | 状态 |
|---|---|
| 修正后的 2026-08-28 固定回归周报 | 已提交 |
| 修正前后图形事件差异及审计表 | 报告内有部分审计，缺独立差异与全量集合闭合 |
| Top 计划三类条件及评价 | 已嵌入 |
| 15 条 L3 冲突逐条回归结果 | 未提交；本次评审已独立复核 |
| 经批准的状态机和旧状态映射 | 仅有合同与映射，缺批准凭据 |
| glossary/field dictionary 覆盖报告 | 未提交，且实测不通过 |
| 普通交易日日报样例 | 未提交 |
| 完整五交易日周报样例 | 已提交 |
| 节假日短周样例 | 未提交 |
| S3/正式候选/触发与失效历史样例 | 未提交 |
| 自动检查结果、真实浏览器截图/日志 | 未提交 |
| 生成、发布时间和调度状态 | 已嵌入；本期为延迟发布 |

## 七、再次提交前的最小整改清单

1. 统一 `pattern_snapshot_count`、`pattern_embedded_count` 和实际数组；若 HTML 只交付 51 条，就不得声明嵌入 493 条。比较审计同时公开前后来源总数、前后嵌入数，并使 `comparable/excluded/added/removed/changed` 可按同一公式闭合。
2. 真正双写 result8 规定的六个价资字段；修正 30 个风险背离与 6 个日报背离的冲突，确保字典公式、实际窗口和结果一致。
3. 对全部实际/允许枚举做构建阻断检查，补齐本评审列出的缺项；页面统一走中文 label，不直接渲染内部代码或 JSON 键。
4. 将旧 `invalidation_level/invalidation_condition` 标记为 deprecated，明确兼容期限和唯一新字段消费规则。
5. 重做风险分组：5 个新进入 S5/S6 的行业必须进入新增/恶化；默认只展示分组重点，全量 90 行作为可展开追溯表。
6. 补齐 B/C 池证据字段与入池/升级/降级/退出，补齐日资金其余四类榜单，以及龙头 `alignment_reason`。
7. 扩展字段字典到所有页面字段，并自动验证“页面字段集合 ⊆ 字典字段集合”；不得用不存在的字段名占位。
8. 将本次可复现问题纳入语义审计。修复前 `semantic_audit_status` 和 `publish_status` 必须为 FAIL；提交四类负例测试及测试输出。
9. 使周五日报迁移与周报保留记录采用同一规范对象，或给出明确投影版本并修改“逐字段一致”的验收口径。
10. 提交状态机批准记录、普通日报、短周、S3/正式候选/触发失效历史样例，以及 15 标签真实浏览器截图与控制台日志。

完成上述 P0 和 P1 缺口后，才可申请“二次优化整体通过”。当前不需要推翻报告架构，继续按 result8 的增量边界修正即可。

## 八、评审边界

本评审核对了远端 Git 对象、内嵌 JSON、前后版本差异、集合/计数关系、页面渲染代码和静态 DOM 合同；没有重算上游行情、供应商资金或历史统计，也没有把报告的研究结论转换为投资建议。真实浏览器验收被判为“未举证”，依据是提交物中没有浏览器产物，且报告明确声明其渲染审计基础仅为 `OFFLINE_HTML_TEMPLATE_CONTRACT`。
