# 系统改造前业务整理框架

> 适用场景：已运行多年、需求持续追加、知识分散，且拟从 Oracle 迁移至 MySQL 的存量系统。
>
> 本框架的目标不是单纯“补文档”，而是建立一套可追溯、可验证、可用于设计与验收的现状基线（As-Is Baseline），为范围确认、方案设计、工作量评估、数据库迁移、回归测试、切换及验收提供共同依据。

## 1. 梳理目标

系统梳理完成后，应能够回答以下问题：

1. 系统现在为谁、在什么场景下解决什么业务问题？
2. 每个业务流程和功能的边界、规则、异常及业务口径是什么？
3. 每项功能由哪些页面、服务、接口、作业、报表、配置、代码和数据库对象共同实现？
4. 数据从哪里产生、如何流转、如何加工、在哪里保存、被谁使用、保留多久？
5. 哪些功能仍在使用，哪些可以下线、合并或重构？
6. Oracle 特性、历史数据质量、性能和外围依赖会给 MySQL 迁移带来什么影响？
7. 改造前后的业务结果如何证明一致，哪些差异是经过确认的目标变化？
8. 每个结论来自什么证据，由谁确认，尚有哪些未知项和待决策项？

### 1.1 梳理原则

- **先现状、后方案**：现状事实、问题判断、目标方案分开记录，避免在梳理阶段无意改变需求。
- **业务与实现双向追溯**：既从业务功能追到页面、代码、SQL 和表，也从表、存储过程、作业反查其业务用途。
- **以证据为准**：访谈结论应尽可能由运行界面、生产脱敏样例、日志、代码、配置、SQL 或制度文件佐证。
- **主流程与例外同等重要**：历史系统的迁移风险往往集中在补偿流程、人工处理、月末批次和边界数据。
- **不确定性显式管理**：未知、冲突和推测不得写成确定事实，应进入问题与决策台账。
- **分层推进**：先完成全量普查，再对核心、高风险、高复杂度功能进行深度梳理。
- **安全合规**：仓库中只保存脱敏样例；不得提交口令、连接串、密钥、生产个人信息或完整生产数据。

## 2. 梳理对象与编号体系

### 2.1 分层模型

建议按以下层级组织范围：

| 层级 | 对象 | 示例 | 主要成果 |
|---|---|---|---|
| L0 | 系统 | 客户服务系统 | 系统边界、上下文、总体架构 |
| L1 | 业务域/模块 | 客户管理、账务管理 | 业务域说明、能力地图 |
| L2 | 端到端流程 | 客户开户、账单结算 | 流程图、参与角色、业务事件 |
| L3 | 功能/用例 | 新增客户、账单冲正 | 单功能档案、验收场景 |
| L4 | 实现单元 | 页面、接口、作业、报表、规则、表、SQL | 技术映射及迁移清单 |

### 2.2 统一编号

统一编号是建立追溯关系的基础。建议至少使用以下前缀：

| 对象 | 编号示例 |
|---|---|
| 业务域/模块 | `MOD-CUST-001` |
| 业务流程 | `PROC-CUST-001` |
| 功能点 | `FUN-CUST-001` |
| 业务需求 | `REQ-CUST-001` |
| 业务规则 | `RULE-CUST-001` |
| 数据实体/表 | `DATA-CUST-001` |
| 接口 | `API-CUST-001` |
| 批处理/调度作业 | `JOB-CUST-001` |
| 报表/指标 | `RPT-CUST-001` |
| SQL/数据库程序 | `SQL-CUST-001` |
| 问题/风险 | `ISSUE-CUST-001` / `RISK-CUST-001` |
| 决策 | `DEC-CUST-001` |
| 验收场景 | `AC-CUST-001` |

编号一旦发布不再复用。对象被废弃时保留编号并标记状态，避免历史引用失效。

### 2.3 最低追溯链

每个核心功能至少应建立以下链路：

```text
业务目标/制度依据
  → 业务流程
  → 功能与业务规则
  → 页面/接口/作业/报表
  → 应用代码与配置
  → 表/视图/SQL/存储过程等数据库对象
  → 测试场景与预期结果
  → 责任人、证据与确认记录
```

## 3. 系统级成果清单

以下成果描述整个系统，仅靠单个功能档案无法替代。

| 序号 | 成果 | 核心内容 | 推荐形式 |
|---|---|---|---|
| 1 | 系统现状说明 | 建设背景、业务目标、服务对象、部署范围、生命周期、当前痛点 | Markdown/Word |
| 2 | 系统边界与上下文图 | 系统内外边界、用户、上下游系统、外部机构、数据流向 | Context 图 |
| 3 | 业务能力与功能地图 | 业务域、模块、流程、功能的层级，以及使用/废弃/待确认状态 | 分层清单 + 能力图 |
| 4 | 端到端业务流程集 | 跨部门、跨模块、跨系统的正常、异常、补偿和人工流程 | BPMN/泳道图 |
| 5 | 组织、用户与权限矩阵 | 组织层级、角色、岗位、功能权限、数据权限、授权及复核机制 | 矩阵 |
| 6 | 业务规则总表 | 规则编号、定义、优先级、适用范围、生效时间、实现位置 | 规则台账 |
| 7 | 数据资产目录 | 业务实体、主数据、交易数据、历史数据、数据库对象及负责人 | 数据目录/数据字典 |
| 8 | 接口与文件交换目录 | 上下游、协议、方向、频率、数据口径、安全和 SLA | 接口台账 |
| 9 | 批处理与调度目录 | 作业链、依赖、时间窗口、重跑方式、处理规模、脚本位置 | 作业台账 + 依赖图 |
| 10 | 报表与指标目录 | 报表用途、指标定义、统计口径、数据来源、使用人 | 报表台账 |
| 11 | 非功能现状基线 | 容量、性能、可用性、安全、审计、备份、容灾、归档 | 指标表 + 证据 |
| 12 | 运行运维基线 | 部署拓扑、版本、参数、监控、告警、日常操作、应急流程 | 架构图 + 运维手册 |
| 13 | 源代码与制品清单 | 仓库、分支、模块、构建方式、依赖、部署包、配置来源 | 制品台账 |
| 14 | Oracle 使用与迁移影响清单 | 对象、特有语法、PL/SQL、数据类型、性能及语义风险 | 专项评估表 |
| 15 | 数据质量画像 | 数据量、增长率、空值、重复、孤儿数据、非法值、异常历史 | 分析报告 + 校验 SQL |
| 16 | 问题、风险、假设与决策台账 | 未知项、冲突项、风险、临时假设、决策及到期时间 | RAID/决策日志 |
| 17 | 改造范围建议 | 保留、修复、重构、合并、下线、暂缓项及依据 | 范围矩阵 |
| 18 | 验收与迁移基线 | 黄金样例、对账口径、回归范围、性能基线、切换约束 | 测试基线包 |

## 4. 每个业务/功能点必须形成的成果

每个功能点建立一份“功能档案”。下列内容应按适用性填写；不适用项写明“无/不适用及原因”，不能直接删除。

### 4.1 基本身份与范围

| 字段 | 要求 |
|---|---|
| 功能编号、名称 | 唯一且与功能台账一致 |
| 所属业务域、流程 | 关联 `MOD-*`、`PROC-*` |
| 功能状态 | 使用中、受限使用、停用但保留数据、待下线、待确认 |
| 功能类型 | 查询、维护、审批、交易、计算、批处理、接口、报表、管理等 |
| 使用角色/组织 | 发起者、处理者、审批者、查询者、运维者及数据范围 |
| 业务负责人/确认人 | 能够确认业务口径的人，不只填写开发人员 |
| 技术负责人 | 熟悉代码、数据库或运行方式的人 |
| 使用频率与时间窗口 | 实时/每日/月末/偶发，业务高峰和不可用窗口 |
| 重要级别 | 核心、重要、一般；注明中断影响和最大可容忍时间 |
| 梳理状态 | 未开始、梳理中、待确认、已确认、基线冻结 |
| 证据更新时间 | 防止把过期截图或旧代码当成当前事实 |

还应明确：

- 本功能包含什么、不包含什么；与相近功能如何区分。
- 入口在哪里，谁有权使用，是否存在菜单不可见但仍被接口或作业调用的情况。
- 当前实际使用情况应由访问日志、数据更新时间或用户确认佐证。
- 是否存在新旧两套实现、灰度逻辑、地区/客户定制或特定日期前后的不同规则。

### 4.2 业务需求成果

每个功能的业务需求至少包括：

1. **业务目的与价值**：解决的问题、产生的业务结果，以及不执行的影响。
2. **需求来源**：制度、合同、监管文件、原始需求单、会议纪要、生产事件或用户提出；记录版本和日期。
3. **参与者**：人员、岗位、组织、外部机构和上下游系统。
4. **触发条件**：用户操作、业务事件、定时调度、消息、文件到达等。
5. **前置条件**：权限、数据状态、依赖任务、营业日、参数开关等。
6. **输入与输出**：业务含义、必填性、来源、格式、单位、精度、默认值、去向。
7. **主成功场景**：按时间顺序描述业务步骤和系统响应。
8. **备选及异常场景**：校验失败、重复提交、并发、超时、部分成功、撤销、冲正、补录、重跑和人工干预。
9. **后置状态**：成功、失败或取消后，业务状态、数据、通知和后续任务如何变化。
10. **约束与边界**：金额、数量、日期、精度、时区、字符、组织范围、历史日期、结账期等边界。
11. **合规与审计**：敏感数据、留痕、审批、职责分离、保存期限、脱敏要求。
12. **业务验收标准**：用可观察、可计算的条件描述“正确”，避免仅写“功能正常”。
13. **当前痛点与期望**：事实问题与目标诉求分列，注明是否纳入本次改造。

建议采用“场景化需求”，例如：

```text
场景 AC-CUST-001：重复请求处理
假设：同一外部流水号已成功入账
当：上游在 5 分钟内使用相同流水号重发相同请求
则：系统不得重复入账，返回首次处理结果，并记录一次重复请求审计日志
```

### 4.3 业务逻辑与规则成果

业务逻辑不能只藏在流程叙述或 SQL 中。应分别输出：

- **流程图/泳道图**：展示角色、系统、顺序、分支、等待、补偿和人工节点。
- **状态机**：列出状态、允许的迁移、触发事件、守卫条件、不可逆状态及超时处理。
- **业务规则表**：规则编号、条件、结果、优先级、例外、生效/失效时间、维护者和实现位置。
- **决策表**：当多个条件组合决定结果时，用决策表消除自然语言歧义。
- **计算公式**：变量定义、单位、精度、中间舍入、最终舍入、负值和空值处理、示例。
- **时序与日历规则**：自然日/工作日、时区、日切、月结、节假日和补跑规则。
- **一致性规则**：事务边界、幂等键、唯一性、并发冲突、锁、重试及补偿方式。

业务规则总表建议包含：

| 规则编号 | 规则名称 | 条件 | 结果 | 优先级/冲突处理 | 例外 | 生效期 | 实现位置 | 依据 | 确认人 |
|---|---|---|---|---|---|---|---|---|---|

特别标注规则当前位于何处：应用代码、数据库触发器/存储过程、配置表、规则引擎、脚本，还是人工操作。相同规则若有多处实现，应记录一致性风险。

### 4.4 页面、交互与人工操作成果

对有界面的功能，应保存：

- 页面入口、菜单路径、URL/路由、页面编号和适用角色。
- 脱敏截图或录屏，覆盖初始、成功、失败、无数据、边界权限等关键状态。
- 字段清单：标签、业务含义、控件、是否必填、默认值、校验、联动、显示/编辑权限、数据库/API 映射。
- 按钮及操作：出现条件、二次确认、后台调用、成功结果、错误提示、防重复提交。
- 页面跳转、弹窗、导入导出、打印、附件和浏览器兼容要求。
- 页面之外的人工步骤：线下审批、表格加工、手工改库、文件传递、电话确认等。

手工改库和线下补偿必须单独登记为风险，记录操作人、授权、频率、脚本、复核、审计和可替代方案。

### 4.5 数据成果

每个功能应形成业务视角和物理视角两部分数据说明。

#### 业务视角

- 涉及的业务实体及其生命周期。
- 数据所有者、维护者、使用者和权威来源（System of Record）。
- 主数据、参考数据、交易数据、派生数据、临时数据的区分。
- 数据创建、读取、更新、删除/作废、归档和清理规则。
- 敏感等级、脱敏、加密、审计和保存期限。

#### 物理视角

- 涉及的表、列、主键、唯一键、外键、索引、分区、约束及默认值。
- 视图、物化视图、序列、同义词、数据库链接、触发器、函数、过程、包等依赖。
- 应用字段/API 字段到数据库字段的映射。
- 表级 CRUD 矩阵和关键字段的数据血缘。
- 当前数据量、年/月增量、峰值、冷热分布、最大行宽及大字段情况。
- 空值、重复、孤儿、非法枚举、越界日期、乱码、超长、精度异常等质量问题。
- 历史数据中规则版本变化和“同字段不同含义/同含义不同字段”的情况。

功能级 CRUD 矩阵示例：

| 功能编号 | 表/视图 | C | R | U | D/作废 | 关键条件 | 事务边界 | 备注 |
|---|---|---:|---:|---:|---:|---|---|---|

### 4.6 接口、消息与文件交换成果

每个接口应输出：

- 接口编号、业务用途、提供方、消费方、负责人、环境和当前状态。
- 协议与形式：HTTP/RPC、消息队列、数据库共享、SFTP 文件等。
- 触发方式、方向、同步/异步、频率、时间窗口和峰值流量。
- 请求/响应或文件字段定义，以及业务字段到应用、数据库字段的映射。
- 字符集、时区、日期格式、金额单位、精度、枚举和空值约定。
- 认证授权、签名、加密、敏感字段处理和网络要求。
- 幂等键、排序、重复、乱序、丢失、超时、重试、熔断、补偿、死信和人工处理。
- 错误码、错误语义、调用日志、链路标识、监控告警和 SLA。
- 脱敏请求/响应样例、契约文件及联调方式。
- 数据库切换期间是否需要兼容新旧数据格式、暂停窗口或消息积压处理。

### 4.7 批处理、调度和脚本成果

多年运行的系统通常有大量“看不见的业务”存在于调度平台和脚本中。每个作业应记录：

- 作业编号、业务目的、调度平台、执行账号、脚本/程序路径和负责人。
- 触发规则、业务日期、日历、时区、时间窗口及上游/下游依赖。
- 输入、输出、临时文件、控制文件、落库表和通知对象。
- 正常处理逻辑、分片/并行方式、提交批次、事务边界和锁影响。
- 成功标准、退出码、日志位置、监控、超时和告警阈值。
- 失败后的重试、断点续跑、幂等、数据清理、回滚和人工补偿步骤。
- 当前数据量、典型/峰值耗时、资源消耗及必须完成的最晚时间。
- 使用的 SQL、Oracle 工具或特性，如 SQL*Loader、SQL*Plus、外部表、DB Link 等。

除单作业说明外，还应输出一张全局作业依赖 DAG，重点识别循环依赖、隐式等待、共享表竞争和月末关键路径。

### 4.8 报表与指标成果

每张报表或每项关键指标应记录：

- 使用目的、使用角色、生成频率、发布范围和是否用于监管/财务/对账。
- 指标业务定义、统计范围、过滤条件、分组维度、计算公式、单位、精度和舍入。
- 数据截止点、业务日期、时区、迟到数据和补录后的重算规则。
- 数据来源、加工链路、临时表/汇总表、核心 SQL 和导出格式。
- 与其他报表中同名指标的口径差异。
- 一组经业务确认的输入数据和预期结果，作为迁移后的黄金样例。

### 4.9 代码、配置、资料和脚本成果

每个功能必须能定位到实现资产，而不是只写“见代码”。至少记录：

| 资产类别 | 应记录内容 |
|---|---|
| 源代码 | 仓库、分支/标签、模块、包/类/方法、版本、构建方式 |
| SQL 映射 | SQL 编号、ORM/Mapper 文件、动态条件、调用入口 |
| 数据库程序 | Schema、对象名、类型、DDL、调用方、依赖对象 |
| 配置 | 配置项、业务含义、默认值、环境差异、维护方式、生效机制 |
| 脚本 | 路径、运行账号、参数、输入输出、调用频率、错误处理 |
| 制品 | 包名、版本、制品库、校验值、部署目标 |
| 业务资料 | 制度、需求单、原型、操作手册、会议纪要、培训材料及版本 |
| 运行证据 | 脱敏日志、监控截图、审计记录、历史故障单及时间范围 |

脚本类成果建议分为四类并纳入版本控制：

1. **提取脚本**：对象、结构、依赖、数据量、数据质量和性能基线采集。
2. **迁移脚本**：MySQL DDL、数据转换、初始化和增量同步。
3. **验证脚本**：行数、主键、汇总值、哈希/抽样、业务规则和性能对比。
4. **回退/清理脚本**：在明确前置条件和影响范围下恢复或清理本次执行产生的数据。

每个可执行脚本应包含用途、适用环境、依赖、参数、执行顺序、预计耗时、幂等性、风险、成功判据、回退方法、作者和复核人。生产环境执行前应经过评审并保留执行记录。

### 4.10 非功能与运行基线成果

每个核心功能至少记录：

- 典型、P95/P99 和峰值响应时间；批处理典型及峰值耗时。
- 日均/峰值请求量、并发数、数据规模和增长率。
- 可用时间、RTO、RPO、降级方式和业务连续性要求。
- 权限、审计、数据保护、漏洞和合规要求。
- 日志、指标、链路追踪、告警条件和生产排障入口。
- 已知容量瓶颈、慢 SQL、锁等待、热点表和历史重大故障。

性能基线应注明采集环境、时间、数据规模和 SQL 执行计划；脱离这些条件的单一耗时没有可比性。

### 4.11 测试与验收成果

每个功能应形成可复用的验收资产：

- 主流程、分支、异常、边界、权限、并发和补偿场景。
- 已脱敏的输入样例、前置数据、操作步骤、预期业务结果和预期数据变化。
- 关键表的前后快照或验证 SQL。
- Oracle 现网输出作为基线的“黄金数据集”，以及其业务确认记录。
- 新旧系统结果比对规则：必须完全一致的字段、允许差异的字段、容差和差异原因。
- 接口契约测试、批处理重跑测试、报表口径测试和性能基准。
- 数据迁移后的完整性、准确性、一致性、唯一性、及时性和可追溯性检查。
- 缺陷严重级别、验收门槛、遗留项处理和签字人。

### 4.12 证据、问题和确认成果

每个结论都应可追溯。建议在功能档案末尾维护：

| 类型 | 编号 | 内容/结论 | 来源或证据 | 责任人 | 截止日期 | 状态 |
|---|---|---|---|---|---|---|
| 问题 | `ISSUE-*` | 尚未确认的问题 | 访谈/代码/数据 |  |  |  |
| 风险 | `RISK-*` | 概率、影响及应对 | 分析材料 |  |  |  |
| 假设 | `ASM-*` | 暂时采用的前提 | 采用原因 |  |  |  |
| 决策 | `DEC-*` | 选择、理由及影响 | 评审纪要 | 决策人 |  |  |

确认记录应包含评审范围、版本/提交号、参与人、日期、结论和附带条件。口头确认应转成可留存记录。

## 5. Oracle → MySQL 迁移专项成果

数据库迁移不只是语法替换。必须同时确认结构、数据、行为、性能和运维方式是否保持或按目标变化。

### 5.1 Oracle 对象全量盘点

按 Schema 输出以下对象及依赖关系：

- 表、列、约束、索引、分区、LOB、临时表和外部表。
- 视图、物化视图及其刷新策略。
- 序列、同义词、数据库链接、目录对象。
- 触发器、函数、存储过程、包、类型和定时任务。
- 用户、角色、对象权限和系统权限。
- 统计信息、执行计划、Hint、Profile 等性能相关配置。
- 无效对象、长期未访问对象、空表、重复对象和疑似废弃对象。

每个对象必须关联到业务功能、接口、作业或报表；无法关联的对象进入“孤立对象待确认清单”，不得直接遗漏或迁移。

对象处置分类建议统一为：

| 分类 | 含义 |
|---|---|
| 自动转换 | 工具可转换，仍需编译和结果验证 |
| 人工改写 | 有明确等价实现，需要人工修改和测试 |
| 重新设计 | MySQL 无直接等价能力或现有设计不适合照搬 |
| 保留在外部 | 暂由其他平台/服务承接，需明确边界和 SLA |
| 下线 | 已确认无业务用途，保留证据和审批 |
| 待确认 | 业务用途或处置未明确，不能进入删除范围 |

### 5.2 兼容性与语义差异矩阵

至少逐项检查：

| 类别 | 重点检查内容 |
|---|---|
| 数据类型 | `NUMBER` 精度、`DATE/TIMESTAMP`、`CHAR/VARCHAR2`、`CLOB/BLOB`、`RAW`、布尔替代、超大字段 |
| 空值语义 | Oracle 将空字符串视为 `NULL`，MySQL 不同；拼接、比较、唯一键和默认值均可能受影响 |
| 字符规则 | 字符集、字节/字符长度、尾随空格、大小写、排序规则、中文排序和索引长度 |
| 日期时间 | `SYSDATE`、时区、零日期、格式转换、月份加减、闰年、日切及精度 |
| 数值计算 | 隐式转换、除法、溢出、小数精度、舍入和聚合结果 |
| 主键生成 | Sequence/Trigger 到自增、号段或业务发号服务；并发、回填和跨表一致性 |
| SQL 语法 | `NVL`、`DECODE`、`ROWNUM`、`CONNECT BY`、`MERGE`、`MINUS`、`DUAL`、旧式外连接、分析函数等 |
| PL/SQL | 包变量、游标、异常、自治事务、动态 SQL、批量处理和返回结果 |
| 事务并发 | 隔离级别、锁粒度、读一致性、死锁、隐式提交、DDL 行为、超时和重试 |
| 索引与约束 | 唯一键空值、函数索引、外键、约束启停、不可见索引及索引选择性 |
| 分页与排序 | 无明确 `ORDER BY` 的结果不保证顺序；分页稳定性和大偏移性能 |
| 分区与归档 | 分区键、分区裁剪、全局/局部索引替代、在线维护方式 |
| 跨库能力 | DB Link、同义词、跨 Schema 访问的替代架构及一致性影响 |
| 批量装载 | SQL*Loader、外部表、导入导出工具的替代及字符/日期处理 |
| 运维能力 | 备份恢复、复制、高可用、统计信息、在线 DDL、容量和监控差异 |

矩阵中每项差异都应关联受影响的 `FUN-*`、`SQL-*`、测试用例、处置方案和确认状态。

### 5.3 数据画像与清洗成果

迁移前按表和关键业务实体输出：

- 总量、有效量、历史量、增长趋势、最大/最小业务日期。
- 主键/业务键重复、空值率、枚举分布、关联完整性和异常字符。
- Oracle 可接受但 MySQL 目标结构无法直接承载的数据。
- 需要清洗、修复、补默认值、截断、转码或归档的数据及业务批准。
- 清洗前后数量、金额等关键汇总的对账结果。

所有修复规则应先经过业务确认，并以可重复执行、可审计、可验证的脚本实施，不能在迁移过程中临时手工改数。

### 5.4 数据映射与迁移规则成果

建立字段级映射表，至少包含：

| 源 Schema/表/列 | 源类型 | 目标库/表/列 | 目标类型 | 转换/清洗规则 | 默认/空值规则 | 主外键策略 | 敏感等级 | 校验规则 | 负责人 |
|---|---|---|---|---|---|---|---|---|---|

同时明确：

- 全量、增量和最终停机增量的范围与水位标识。
- 插入顺序、外键依赖、并行度、批次大小、断点续传和失败重跑。
- 新旧主键关系、序列当前值、自增起点及迁移期间新增数据的冲突规避。
- 删除、逻辑删除、迟到数据、历史修订和大字段的处理。
- 迁移窗口、预计耗时、业务冻结点和最大允许停机时间。

### 5.5 SQL 与数据库程序改造成果

每条核心或高风险 SQL/程序至少保留：

- 原 Oracle 版本、调用场景、参数、业务含义和基准数据规模。
- MySQL 改造版本及语义差异说明。
- 结果集比对：行数、键集合、字段值、汇总值、顺序和空值。
- 执行计划、索引使用、典型/P95 耗时和资源消耗对比。
- 并发、事务、锁等待、死锁和失败恢复验证。
- 代码评审、测试结论和关联的功能/规则编号。

禁止仅凭“SQL 可执行”判定迁移完成；可执行不代表业务结果、事务行为和性能等价。

### 5.6 对账、切换与回退成果

应在改造开始前确定：

- **技术对账**：对象数、行数、主键集合、空值、哈希/抽样、LOB、约束和索引。
- **业务对账**：按业务日、机构、状态、币种等维度核对笔数、金额、余额、指标和报表。
- **差异分级**：零容差项、可容差项、已批准差异项及人工复核项。
- **切换步骤**：停止写入、最终增量、验证、应用切换、开放业务、观察和确认。
- **放行条件**：必须满足的自动检查、业务签字、性能和监控指标。
- **回退触发条件**：何种差异、错误率、性能或时间超限必须回退，由谁决策。
- **回退方案**：应用、数据库、消息、文件和外部系统如何恢复，切换后新增数据如何处置。
- **演练记录**：每次全量/增量/切换演练的版本、数据量、耗时、问题和改进。

## 6. 建议的单功能档案模板

每个 `FUN-*` 可按以下目录填写。表格字段可扩展，但不应省略适用项。

```markdown
# FUN-XXX-000 功能名称

## 1. 文档信息
- 状态/版本/更新时间：
- 业务负责人/技术负责人/确认人：
- 所属模块/流程：
- 关联需求/规则/接口/作业/报表：

## 2. 功能概述与边界
- 业务目的：
- 使用角色与场景：
- 包含/不包含：
- 使用频率、关键时段、业务影响：
- 当前状态及证据：

## 3. 业务流程
- 触发及前置条件：
- 主流程：
- 分支流程：
- 异常、撤销、补偿和人工流程：
- 后置状态：
- 流程图/状态图：

## 4. 业务规则
| 规则编号 | 条件 | 结果 | 例外 | 生效期 | 依据 | 实现位置 |

## 5. 输入、输出与页面
| 字段 | 业务含义 | 来源/去向 | 必填 | 格式/单位/精度 | 默认值 | 校验/转换 | 权限 | 数据映射 |

## 6. 数据与事务
- 业务实体及生命周期：
- 表/视图 CRUD 矩阵：
- 字段与血缘：
- 事务、锁、幂等、并发和一致性：
- 数据量、增长和质量问题：
- 敏感、审计、归档和清理：

## 7. 接口、消息和文件
- 契约、样例、调用关系、SLA：
- 超时、重试、重复、乱序和补偿：

## 8. 批处理、报表和人工操作
- 调度、依赖、时间窗口和重跑：
- 指标口径及验证样例：
- 人工步骤及风险：

## 9. 实现资产
| 类型 | 仓库/Schema | 文件/对象 | 版本 | 用途 | 依赖 |

## 10. Oracle → MySQL 影响
- 对象/语法/类型/事务/性能差异：
- 处置分类及方案：
- 数据清洗与迁移规则：
- 关联验证脚本：

## 11. 非功能基线
- 容量、性能、可用性、安全、审计和监控：

## 12. 测试与验收
| 场景编号 | 前置数据 | 操作/输入 | 预期业务结果 | 预期数据变化 | 优先级 |

## 13. 问题、风险、假设与决策
| 类型/编号 | 内容 | 影响 | 责任人 | 截止日期 | 状态/结论 |

## 14. 证据与确认
| 证据编号 | 类型 | 文件/链接/提交号 | 采集日期 | 提供人 | 适用范围 |

- 评审版本、日期、参与人、结论及附带条件：
```

## 7. 功能台账与多维矩阵

单功能文档之外，应维护可筛选的总台账，以便估算和管控。

### 7.1 功能总台账字段

建议至少包含：

```text
功能编号、功能名称、业务域、业务流程、功能状态、业务负责人、技术负责人、
使用角色、使用频率、关键时段、业务重要度、当前使用证据、上游、下游、
页面数、接口数、作业数、报表数、涉及表数、涉及数据库程序数、数据量级、
Oracle 特性等级、数据质量等级、改造建议、迁移复杂度、知识缺口、风险等级、
梳理状态、确认状态、目标版本、备注
```

### 7.2 必备关系矩阵

- 功能 × 角色/权限矩阵。
- 流程 × 功能矩阵。
- 功能 × 页面/API/作业/报表矩阵。
- 功能 × 表/视图 CRUD 矩阵。
- 功能 × 业务规则矩阵。
- 功能 × 测试场景矩阵。
- Oracle 对象 × 业务功能 × 目标处置矩阵。
- 上下游系统 × 接口 × 切换影响矩阵。

矩阵用于发现孤立项：没有功能归属的表/程序、没有实现位置的规则、没有验收用例的功能、没有消费方的接口等。

## 8. 梳理实施方法

### 8.1 阶段一：准备与全量普查

1. 确认系统边界、参与人、环境、只读权限、资料入口和脱敏规则。
2. 汇总已有需求、设计、操作手册、故障单、代码库、数据库字典、调度和监控资料。
3. 从菜单、路由、接口网关、调度平台、报表平台、代码和数据库对象多入口盘点。
4. 建立功能、接口、作业、报表、表和 Oracle 对象总台账。
5. 标识使用中、疑似废弃、无法确认和高风险对象。

阶段成果：范围基线、全量台账、负责人清单、第一版上下文图、问题清单。

### 8.2 阶段二：重点功能深度梳理

优先选择业务影响高、数据库复杂、数据量大、跨系统依赖多、历史故障多或知识即将流失的功能：

1. 业务访谈与操作演示。
2. 代码、配置、SQL 和数据库对象静态分析。
3. 运行日志、监控、数据样本和执行计划验证。
4. 补齐流程、规则、状态、数据血缘和异常处理。
5. 形成黄金样例与现状性能基线。
6. 识别 Oracle → MySQL 差异并初步分类处置。

阶段成果：完整功能档案、迁移影响评估、验证脚本和风险清单。

### 8.3 阶段三：交叉核验与范围决策

- 用“业务自上而下”和“技术资产自下而上”结果互相核对。
- 组织业务、开发、测试、数据、运维和安全联合评审。
- 对保留、修复、重构、合并、下线和暂缓项作出书面决策。
- 将未知项分配责任人和截止日期，重大未知不得带入实施基线。
- 冻结经确认的现状版本；后续变化走变更流程。

### 8.4 三遍法，控制多年系统的梳理成本

- **第一遍——普查**：追求全量覆盖，快速确认“有没有、谁负责、大致风险”。
- **第二遍——深挖**：对核心和高风险项补齐字段级、规则级、SQL 级成果。
- **第三遍——验证**：通过演示、数据、代码、日志和测试样例交叉证实并签字。

## 9. 优先级与风险分级

建议每项按 1～5 分评估以下维度：

- 业务影响：中断、错账、监管、客户或财务影响。
- 数据风险：数据量、质量、敏感性、历史跨度和可恢复性。
- Oracle 依赖：PL/SQL、复杂 SQL、DB Link、分区、触发器等使用程度。
- 技术复杂度：跨模块、事务、并发、性能和部署复杂度。
- 外部依赖：上下游数量、协调难度和切换窗口。
- 知识缺口：文档缺失、负责人离岗、用途无法确认。
- 变更频率与故障历史：需求活跃度、事故和人工干预频率。

可按组织偏好加权形成风险分，但必须同时保留各维度原始分数和评分依据，避免总分掩盖单项致命风险。具备监管/财务影响、不可逆数据风险或用途不明的数据库对象，应直接进入重点评审，不依赖平均分。

## 10. 角色与职责建议

| 角色 | 主要职责 |
|---|---|
| 业务负责人 | 确认目标、流程、规则、口径、范围和业务验收标准 |
| 一线业务代表 | 演示真实操作，补充例外、人工处理和高峰场景 |
| 产品/业务分析 | 主持梳理，维护功能档案、流程、规则和追溯关系 |
| 应用开发 | 定位代码、配置、SQL、接口和实现差异 |
| DBA/数据工程 | 对象盘点、数据画像、兼容评估、迁移与对账脚本 |
| 测试 | 将现状转化为回归、数据验证和性能验收资产 |
| 运维/SRE | 提供部署、调度、监控、容量、故障和切换约束 |
| 安全/合规 | 确认权限、审计、敏感数据、留存和合规要求 |
| 架构负责人 | 处理跨域边界、目标处置、重大风险和技术决策 |
| 项目负责人 | 范围、资源、里程碑、风险、决策和基线变更管理 |

任何核心规则、指标口径或下线决定都不应只由技术人员单方确认。

## 11. 仓库目录与文件规范

建议文档、台账和脚本统一版本管理：

```text
system-renovation/
├── 00_总览/
│   ├── 系统现状说明.md
│   ├── 系统边界与上下文图.drawio
│   ├── 功能总台账.xlsx
│   └── 术语表.md
├── 01_业务域/
│   └── MOD-XXX_模块名/
│       ├── 业务域说明.md
│       ├── PROC-XXX_流程名/
│       └── FUN-XXX_功能名/
│           ├── 功能档案.md
│           ├── diagrams/
│           ├── evidence/
│           └── samples/
├── 02_数据/
│   ├── 数据资产目录/
│   ├── 数据字典/
│   ├── 数据血缘/
│   └── 数据质量/
├── 03_接口/
├── 04_作业与报表/
├── 05_数据库迁移/
│   ├── 01_oracle对象盘点/
│   ├── 02_兼容性评估/
│   ├── 03_数据映射/
│   ├── 04_迁移脚本/
│   ├── 05_验证与对账脚本/
│   ├── 06_性能基线/
│   └── 07_切换与回退/
├── 06_测试与验收/
├── 07_问题风险与决策/
└── 99_模板与附件/
```

脚本建议进一步区分：

```text
sql/oracle/extract/      # 只读盘点与基线采集
sql/mysql/ddl/           # 目标结构
sql/migration/transform/ # 转换及装载
sql/validation/          # 技术及业务对账
sql/rollback/            # 经评审的回退脚本
```

文件应包含版本或通过 Git 提交追踪版本；图形文件同时导出可浏览格式。二进制台账如使用 Excel，应明确唯一维护入口，并考虑同步导出 CSV 便于评审和自动检查。

## 12. 单功能“完成定义”（Definition of Done）

一个功能只有同时满足以下条件，才可标记为“梳理完成”：

- [ ] 功能边界、状态、业务负责人和技术负责人明确。
- [ ] 主流程、分支、异常、补偿、人工操作和状态变化已记录。
- [ ] 业务规则、计算口径、优先级、例外和生效期已明确。
- [ ] 页面、接口、作业、报表、代码、配置和资料已关联。
- [ ] 涉及的表、字段、CRUD、血缘、事务和数据生命周期已明确。
- [ ] 相关 Oracle 对象和特有语法已全量识别并给出处置分类。
- [ ] 当前数据量、质量、性能和运行方式有可复查的证据。
- [ ] 主流程及高风险场景有脱敏黄金样例、预期结果和验证 SQL。
- [ ] 新旧结果对账口径、允许差异和性能验收门槛已定义。
- [ ] 未知项、冲突、假设、风险和决策均已登记，无隐含悬而未决事项。
- [ ] 业务、开发、测试、数据/DBA、运维等相关方已按范围评审。
- [ ] 文档版本、证据日期、确认记录和变更历史完整。

## 13. 系统梳理阶段退出标准

建议在改造设计和开发全面展开前设置评审门槛：

1. 功能、接口、作业、报表和 Oracle 对象台账达到约定的全量覆盖率。
2. 所有核心及高风险功能达到单功能完成定义。
3. 所有 Oracle 对象均有业务归属或进入孤立对象审批流程，并有目标处置分类。
4. 关键业务规则、指标口径、数据映射和对账规则已由业务确认。
5. 数据质量问题已有处置原则、责任人和完成时点。
6. 改造范围和非范围项已批准；下线项有证据、依赖检查和审批。
7. 重大未知项均已关闭，或有明确的隔离方案和管理层风险接受。
8. 迁移、验证、切换和回退的可行性至少完成一次代表性验证。
9. 现状基线已冻结，后续需求通过变更记录维护。

## 14. 建议的最终交付包

改造启动前，建议至少形成以下可交接成果包：

1. **一张图**：系统边界、用户、上下游及核心数据流。
2. **一套台账**：功能、规则、数据、接口、作业、报表、代码制品和 Oracle 对象。
3. **一组档案**：核心及高风险功能的完整单功能档案。
4. **一套矩阵**：角色权限、功能实现、数据 CRUD、对象归属和测试追溯矩阵。
5. **一份专项评估**：Oracle → MySQL 兼容性、工作量、风险和处置建议。
6. **一套脚本**：盘点、迁移、清洗、验证、对账和回退脚本及执行说明。
7. **一套基线数据**：脱敏黄金样例、业务对账口径、性能与容量基线。
8. **一本账**：问题、风险、假设、决策、遗留项及签字记录。
9. **一份范围结论**：保留、修复、重构、合并、下线、暂缓及不在范围内的项目。
10. **一套验收依据**：功能、数据、接口、作业、报表、性能和切换验收标准。

这套成果既是前期梳理的交付物，也是后续需求基线、技术设计输入、任务拆分依据、迁移验收标准和运行交接资料。后续任何改造任务都应能回链到功能、规则、数据对象和验收场景；无法回链的改造项应先补充范围和依据，再进入实施。
